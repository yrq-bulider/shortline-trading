# 短线操作 v1.0 快速入门

## 一、3 个常用命令

```bash
# 1. 每日扫描（推荐）
python daily.py

# 2. 切到 T+0 模式（ETF/可转债）
python daily.py --t0

# 3. 只跑回测（看历史胜率）
python daily.py --backtest
```

## 二、产生的文件

| 文件 | 作用 |
|------|------|
| `短线工具箱/今日报告.html` | **主战场**：4维评分、TOP3卡片、历史回测、调参建议 |
| `M.D短线操作.md` | 次日操作计划（T+1 早盘 9:25-9:45 用） |
| `短线工具箱/筛选结果.csv` | 全市场扫描结果，Excel 打开可排序 |
| `短线工具箱/历史评分.jsonl` | 自动累积的评分历史（不要删，回测靠它） |
| `短线工具箱/回测报告.json` | 回测结果 JSON 摘要 |

## 三、第一次跑要做的事

```bash
# 1. 装依赖（v1 新增 akshare 维度）
pip install baostock akshare pandas numpy

# 2. 跑一次扫描（生成第一份历史评分）
python daily.py

# 3. 等 5-10 个交易日后再跑 --backtest 看胜率
python daily.py --backtest
```

## 四、T+1 vs T+0 关键差异

| 维度 | T+1（A股股票） | T+0（ETF/可转债） |
|------|----------------|-------------------|
| 卖出 | 次日 | 当日 14:30 前必清 |
| 止盈 | +5% | +2% |
| 止损 | -5% | -1.5% |
| 核心 | 隔夜发酵（消息催化权重大）| 分时精准（技术权重大）|
| 切换 | 默认 | `daily.py --t0` |

## 五、调参（直接改 v1 文件顶部）

| 想改什么 | 在 `筛选明日股票_v1.py` 改哪里 |
|----------|------------------------------|
| 交易模式 | 第 51 行 `TRADING_MODE = 'T+1'` |
| 评分权重 | 第 67-79 行 `WEIGHT_*` |
| 仓位上限 | `dynamic_position_pct()` 函数 |
| 止损/止盈点 | 第 85-97 行 |
| 行业排除 | 第 72-76 行 `EXCLUDE_INDUSTRIES` |
| 价格区间 | 第 77-78 行 |

## 六、6/5 复盘结论（v1 vs v0.3）

| 维度 | v0.3 | v1.0 |
|------|------|------|
| 选股 | 技术面 1 维 | 技术+业绩+资金+消息 4 维 |
| 兰花科创 | 入选 TOP3（-0.41%）| **业绩地雷直接淘汰** |
| 皖维高新 | 入选 TOP3（+10%）| 综合分 90+，仓位 40% |
| 选股准确率 | ~50% | 预计 70-80%（待回测验证） |

## 七、回测归因：4维评分到底有没有用？

`python daily.py --backtest` 现在输出**归因分析**：
- **相关性表**：每个子分（技术/业绩/资金/消息）vs 次日收益的相关系数
  - `|r| ≥ 0.3` = 强有效
  - `0.15 ≤ |r| < 0.3` = 中等有效  
  - `|r| < 0.1` + 胜率差 < 5% = 噪声（应降权或废弃）
- **消融实验**：4维全开 vs 只看技术/只看业绩/只看资金/只看消息
  - 看哪种策略胜率最高
  - 如果"单维度" > "4维" → 说明维度加权有问题，要调
- **自动调参建议**：基于相关性强弱，建议"哪个维度该加权重、哪个该降"

**最少需要 20 笔历史数据**才靠谱。早期样本 < 5 时只会提示"样本太少"。

## 八、v1.0 核心升级清单

1. **4维评分**：技术+业绩+资金+消息（T+1:30/25/20/25，T+0:50/15/25/10）
2. **业绩地雷排除**：一季报归母净利润同比 -30% 直接淘汰
3. **资金流过滤**：5日主力净流入不为大额净流出
4. **消息催化**：近30日定增/并购/订单加分，减持/亏损/退市减分
5. **板块联动**：同行业平均涨跌 + MA5 方向作为板块强度
6. **智能去重**：近3日推荐过的票自动跳过（防"信号耗尽"）
7. **T+1/T+0 双模式**：权重、规则、模板自动切换
8. **自动回测**：拉历史评分+次日实际涨跌，算胜率+调参建议
9. **回测归因**（新）：验证4维是否有用，给出权重调整建议
10. **HTML 报告**：可视化 4 维子分柱状图 + TOP3 卡片
11. **daily.py 一键入口**：一条命令跑完所有工作
