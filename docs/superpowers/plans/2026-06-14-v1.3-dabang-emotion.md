# v1.3 涨停板候选池 + 大盘情绪门 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 4 维评分之上叠加「大盘情绪门」(冷市自动降仓)与「涨停板候选池」(主板首板小盘机构介入风格),提升短线胜率而不破坏 v1.1/P1 稳定性。

**Architecture:** 两个新模块 `市场情绪.py` 与 `打板池.py` 落到 `短线工具箱/`,各自接收已有的 `zt_pool_df` / `market_activity_df` / 评分结果,返回纯 dict/list。主程序 `筛选明日股票_v1.py` 通过 importlib 套现有模式 import,新增 1 个 `_init_emotion_and_zt_pool()` 预拉缓存,在 TOP3 仓位计算处套 emotion multiplier,在 CLI + HTML 输出尾部追打板池一节。所有失败路径降级到 v1.2 行为(emotion='normal'/multiplier=1.0/打板池空)。

**Tech Stack:** Python 3.x + akshare(`stock_zt_pool_em` / `stock_market_activity_legu`) + pandas + 已有 `call_with_fallback`/`@safe_score`/importlib 模式 + pytest。

---

## File Structure

**New files:**
- `短线工具箱/market_emotion.py` — 计算大盘情绪档位(hot/normal/cool/cold)+ 对应仓位乘数
- `短线工具箱/dabang_pool.py` — 从涨停股池筛选主板首板小盘候选 + 4 维分排序
- `短线工具箱/tests/test_market_emotion.py` — 4 个单元测试
- `短线工具箱/tests/test_dabang_pool.py` — 8 个单元测试

**Modified files:**
- `短线工具箱/akshare_resilient.py` — 追加 `fetch_zt_pool` + `fetch_market_activity` 两个包装器
- `短线工具箱/backtest.py` — 追加 `print_dabang_pool_report` 打印函数
- `筛选明日股票_v1.py`:
  - 第 50-65 行附近: importlib 列表追加新符号
  - 第 802 行后: 追加 `_EMOTION_TIER` 缓存 + `_init_emotion_and_zt_pool()` 函数
  - 主流程 preload 段(LHB 之后): 调一次 `_init_emotion_and_zt_pool()`
  - 第 1769 行: TOP3 仓位计算套 emotion multiplier
  - 第 2128 行附近: HTML 报告新增 dabang section + CLI 打印 dabang 报告
- `README.md` — 新增 §涨停板候选池 + 情绪门一节
- `docs/AGENT_LOG.md` — 追加历史事件

---

## Task 1: 数据层 — `fetch_zt_pool` 包装器

**Files:**
- Modify: `短线工具箱/akshare_resilient.py` (在末尾追加,与 `fetch_lhb_inst` 并列)
- Test: 无单测(纯包装,与现有 `fetch_lhb_inst` 同模式)

- [ ] **Step 1: 在 `短线工具箱/akshare_resilient.py` 末尾追加 `fetch_zt_pool` 包装**

打开 `短线工具箱/akshare_resilient.py`,在文件最后(`fetch_lhb_inst` 之后)追加:

```python
def fetch_zt_pool(date: str | None = None) -> 'pd.DataFrame | None':
    """涨停股池 21 字段(v1.3)。ak.stock_zt_pool_em 默认拉今日。
    date 形如 '20260614'。"""
    import akshare as ak
    if date is None:
        from datetime import datetime
        date = datetime.now().strftime('%Y%m%d')
    return call_with_fallback(
        attempts=[(ak.stock_zt_pool_em, {'date': date})],
        cache_key=f'zt_pool_{date}',
        timeout=10,
        retries=1,
    )
```

- [ ] **Step 2: 验证语法**

Run: `python -m py_compile 短线工具箱/akshare_resilient.py`
Expected: 无输出 + 退出码 0

- [ ] **Step 3: 验证可导入**

Run: `python -c "from 短线工具箱.akshare_resilient import fetch_zt_pool; print('ok')"`
Expected: 输出 `ok`

- [ ] **Step 4: Commit**

```bash
git add 短线工具箱/akshare_resilient.py
git commit -m "feat(akshare): add fetch_zt_pool wrapper for v1.3"
```

---

## Task 2: 数据层 — `fetch_market_activity` 包装器

**Files:**
- Modify: `短线工具箱/akshare_resilient.py`

- [ ] **Step 1: 在 Task 1 的 `fetch_zt_pool` 之后追加 `fetch_market_activity`**

```python
def fetch_market_activity() -> 'pd.DataFrame | None':
    """乐咕乐股市场活跃度(v1.3)。ak.stock_market_activity_legu 返回 1 行
    含'涨停'/'跌停'/'连板'等字段。"""
    import akshare as ak
    return call_with_fallback(
        attempts=[(ak.stock_market_activity_legu, {})],
        cache_key='market_activity',
        timeout=10,
        retries=1,
    )
```

- [ ] **Step 2: 验证语法**

Run: `python -m py_compile 短线工具箱/akshare_resilient.py`
Expected: 退出码 0

- [ ] **Step 3: Commit**

```bash
git add 短线工具箱/akshare_resilient.py
git commit -m "feat(akshare): add fetch_market_activity wrapper for v1.3"
```

---

## Task 3: 市场情绪模块 — 写失败测试

**Files:**
- Create: `短线工具箱/tests/test_market_emotion.py`

- [ ] **Step 1: 写 4 个失败测试**

新建文件 `短线工具箱/tests/test_market_emotion.py`,内容如下:

```python
"""v1.3 大盘情绪门测试 — 4 档(hot/normal/cool/cold)分流。"""
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '..'))

import pandas as pd
import pytest


def _make_zt_df(zt_count: int, lianban_count: int, dt_count: int = 0) -> pd.DataFrame:
    """构造一个最小可用的涨停池 df,只填打分用得到的字段。"""
    rows = []
    for i in range(zt_count):
        rows.append({
            '代码': f'600{i:03d}',
            '连板数': 2 if i < lianban_count else 1,
        })
    for i in range(dt_count):
        rows.append({'代码': f'500{i:03d}', '连板数': -1})
    return pd.DataFrame(rows)


def _make_activity_df(zt_count: int) -> pd.DataFrame:
    return pd.DataFrame([
        {'item': '涨停', 'value': str(zt_count)},
        {'item': '上涨', 'value': '2000'},
        {'item': '下跌', 'value': '2500'},
    ])


def test_hot_tier():
    """连板 ≥30 且 涨停 ≥80 → hot,multiplier=1.0。"""
    from 短线工具箱.market_emotion import compute_emotion_tier
    zt_df = _make_zt_df(zt_count=100, lianban_count=35)
    act_df = _make_activity_df(zt_count=100)
    res = compute_emotion_tier(zt_df, act_df)
    assert res['tier'] == 'hot'
    assert res['multiplier'] == 1.0
    assert res['lianban_count'] >= 30
    assert res['zt_count'] >= 80


def test_cool_tier():
    """连板 <10 → cool,multiplier=0.5。"""
    from 短线工具箱.market_emotion import compute_emotion_tier
    zt_df = _make_zt_df(zt_count=50, lianban_count=5)
    act_df = _make_activity_df(zt_count=50)
    res = compute_emotion_tier(zt_df, act_df)
    assert res['tier'] == 'cool'
    assert res['multiplier'] == 0.5


def test_cold_tier():
    """涨停 <30 → cold,multiplier=0.2,优先级高于连板判断。"""
    from 短线工具箱.market_emotion import compute_emotion_tier
    zt_df = _make_zt_df(zt_count=20, lianban_count=8)
    act_df = _make_activity_df(zt_count=20)
    res = compute_emotion_tier(zt_df, act_df)
    assert res['tier'] == 'cold'
    assert res['multiplier'] == 0.2


def test_zt_pool_none():
    """zt_pool_df 为 None → tier='normal',multiplier=1.0,reason 含'数据缺失'。"""
    from 短线工具箱.market_emotion import compute_emotion_tier
    res = compute_emotion_tier(None, None)
    assert res['tier'] == 'normal'
    assert res['multiplier'] == 1.0
    assert '数据缺失' in res['reason'] or '缺失' in res['reason']
```

- [ ] **Step 2: 验证测试确实失败(模块还没建)**

Run: `pytest 短线工具箱/tests/test_market_emotion.py -v`
Expected: 4 个测试全 FAIL,错误类型 `ModuleNotFoundError: No module named '短线工具箱.market_emotion'`

- [ ] **Step 3: Commit 失败测试**

```bash
git add 短线工具箱/tests/test_market_emotion.py
git commit -m "test(emotion): add failing tests for market emotion tier"
```

---

## Task 4: 市场情绪模块 — 实现使测试通过

**Files:**
- Create: `短线工具箱/market_emotion.py`

- [ ] **Step 1: 创建 `短线工具箱/market_emotion.py`**

```python
"""大盘情绪门(v1.3) — 把当日涨停池/市场活跃度归到 4 档,
返回对应的仓位乘数。失败默认 normal/1.0,不影响 v1.2 行为。"""

EMOTION_THRESHOLDS = {
    'hot':  {'lianban_count': 30, 'zt_count': 80},
    'cool': {'lianban_count': 10},
    'cold': {'zt_count': 30},
}

POSITION_MULTIPLIER = {
    'hot':    1.0,
    'normal': 0.8,
    'cool':   0.5,
    'cold':   0.2,
}


def _count_lianban(zt_pool_df) -> int:
    """涨停池里 连板数 ≥ 2 的票数(首板不算连板)。"""
    if zt_pool_df is None or zt_pool_df.empty:
        return 0
    col = next((c for c in zt_pool_df.columns if '连板' in str(c)), None)
    if col is None:
        return 0
    import pandas as pd
    return int((pd.to_numeric(zt_pool_df[col], errors='coerce') >= 2).sum())


def _count_from_activity(activity_df, key: str) -> int:
    """从 stock_market_activity_legu 的 item/value 表里抽取 '涨停'/'跌停' 等数字。"""
    if activity_df is None or activity_df.empty:
        return 0
    try:
        row = activity_df[activity_df['item'] == key]
        if row.empty:
            return 0
        return int(float(row['value'].iloc[0]))
    except Exception:
        return 0


def compute_emotion_tier(zt_pool_df, market_activity_df) -> dict:
    """4 档分流:
      - cold: 涨停 <30 → multiplier 0.2
      - cool: 连板 <10 → 0.5
      - hot: 连板 ≥30 且 涨停 ≥80 → 1.0
      - normal: 其余 → 0.8
    优先级 cold > cool > hot > normal。数据缺失 → normal/1.0。
    返回 dict: tier/multiplier/zt_count/lianban_count/dt_count/reason。"""
    if zt_pool_df is None and market_activity_df is None:
        return {
            'tier': 'normal', 'multiplier': 1.0,
            'zt_count': 0, 'lianban_count': 0, 'dt_count': 0,
            'reason': '数据缺失,默认 normal',
        }

    lianban_count = _count_lianban(zt_pool_df)
    zt_count = _count_from_activity(market_activity_df, '涨停')
    if zt_count == 0 and zt_pool_df is not None:
        zt_count = len(zt_pool_df)
    dt_count = _count_from_activity(market_activity_df, '跌停')

    if zt_count < EMOTION_THRESHOLDS['cold']['zt_count']:
        tier = 'cold'
    elif lianban_count < EMOTION_THRESHOLDS['cool']['lianban_count']:
        tier = 'cool'
    elif (lianban_count >= EMOTION_THRESHOLDS['hot']['lianban_count']
          and zt_count >= EMOTION_THRESHOLDS['hot']['zt_count']):
        tier = 'hot'
    else:
        tier = 'normal'

    return {
        'tier': tier,
        'multiplier': POSITION_MULTIPLIER[tier],
        'zt_count': zt_count,
        'lianban_count': lianban_count,
        'dt_count': dt_count,
        'reason': f'涨停{zt_count} 连板{lianban_count} 跌停{dt_count} → {tier}',
    }
```

- [ ] **Step 2: 运行测试**

Run: `pytest 短线工具箱/tests/test_market_emotion.py -v`
Expected: 4 个测试全 PASS

- [ ] **Step 3: Commit**

```bash
git add 短线工具箱/market_emotion.py
git commit -m "feat(emotion): implement compute_emotion_tier for v1.3"
```

---

## Task 5: 打板池模块 — 写失败测试

**Files:**
- Create: `短线工具箱/tests/test_dabang_pool.py`

- [ ] **Step 1: 写 8 个失败测试**

新建 `短线工具箱/tests/test_dabang_pool.py`:

```python
"""v1.3 涨停板候选池筛选测试。"""
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '..'))

import pandas as pd
import pytest


def _make_row(code='600001', name='测试票', price=15.0, mv_yi=100.0,
              feng='10:30:00', zhaban=0, lianban=1):
    """构造涨停池单行。"""
    return {
        '代码': code, '名称': name,
        '最新价': price,
        '流通市值': mv_yi * 100_000_000,
        '封板时间': feng,
        '炸板次数': zhaban,
        '连板数': lianban,
    }


def _make_score(code='600001', composite=70, earn=60, falsified=False):
    return {
        'code': code, 'composite': composite,
        'subscores': {'tech': 70, 'earn': earn, 'flow': 70, 'news': 70,
                      'earn_reason': '', 'flow_reason': '', 'news_reason': ''},
        'falsified': falsified,
    }


def test_basic_pass():
    """符合全部硬过滤 + 评分 ≥60 → 入池。"""
    from 短线工具箱.dabang_pool import compute_dabang_candidates
    zt_df = pd.DataFrame([_make_row(code='600001')])
    scores = {'600001': _make_score(code='600001', composite=75)}
    out = compute_dabang_candidates(zt_df, scores, top_n=5)
    assert len(out) == 1
    assert out[0]['code'] == '600001'


def test_filter_high_price():
    """价格 >30 → 过滤掉。"""
    from 短线工具箱.dabang_pool import compute_dabang_candidates
    zt_df = pd.DataFrame([_make_row(code='600002', price=35.0)])
    scores = {'600002': _make_score(code='600002', composite=80)}
    out = compute_dabang_candidates(zt_df, scores, top_n=5)
    assert out == []


def test_filter_big_mv():
    """流通市值 >300 亿 → 过滤掉。"""
    from 短线工具箱.dabang_pool import compute_dabang_candidates
    zt_df = pd.DataFrame([_make_row(code='600003', mv_yi=500.0)])
    scores = {'600003': _make_score(code='600003', composite=80)}
    out = compute_dabang_candidates(zt_df, scores, top_n=5)
    assert out == []


def test_filter_late_feng():
    """封板时间 >14:30 → 过滤掉。"""
    from 短线工具箱.dabang_pool import compute_dabang_candidates
    zt_df = pd.DataFrame([_make_row(code='600004', feng='14:45:00')])
    scores = {'600004': _make_score(code='600004', composite=80)}
    out = compute_dabang_candidates(zt_df, scores, top_n=5)
    assert out == []


def test_filter_lianban_ne_1():
    """连板数 ≠1(首板) → 过滤掉(只要首板)。"""
    from 短线工具箱.dabang_pool import compute_dabang_candidates
    zt_df = pd.DataFrame([_make_row(code='600005', lianban=2)])
    scores = {'600005': _make_score(code='600005', composite=80)}
    out = compute_dabang_candidates(zt_df, scores, top_n=5)
    assert out == []


def test_filter_low_4d():
    """4 维综合分 <60 → 过滤掉。"""
    from 短线工具箱.dabang_pool import compute_dabang_candidates
    zt_df = pd.DataFrame([_make_row(code='600006')])
    scores = {'600006': _make_score(code='600006', composite=55)}
    out = compute_dabang_candidates(zt_df, scores, top_n=5)
    assert out == []


def test_filter_leiqu():
    """业绩雷区(falsified=True) → 过滤掉。"""
    from 短线工具箱.dabang_pool import compute_dabang_candidates
    zt_df = pd.DataFrame([_make_row(code='600007')])
    scores = {'600007': _make_score(code='600007', composite=75, falsified=True)}
    out = compute_dabang_candidates(zt_df, scores, top_n=5)
    assert out == []


def test_zt_pool_none():
    """zt_pool_df 为 None → 返回 []。"""
    from 短线工具箱.dabang_pool import compute_dabang_candidates
    out = compute_dabang_candidates(None, {}, top_n=5)
    assert out == []
```

- [ ] **Step 2: 验证测试失败**

Run: `pytest 短线工具箱/tests/test_dabang_pool.py -v`
Expected: 8 个测试 FAIL,错误 `ModuleNotFoundError: No module named '短线工具箱.dabang_pool'`

- [ ] **Step 3: Commit**

```bash
git add 短线工具箱/tests/test_dabang_pool.py
git commit -m "test(dabang): add failing tests for dabang candidate pool"
```

---

## Task 6: 打板池模块 — 实现使测试通过

**Files:**
- Create: `短线工具箱/dabang_pool.py`

- [ ] **Step 1: 创建 `短线工具箱/dabang_pool.py`**

```python
"""涨停板首板小盘候选池(v1.3)。从涨停股池里筛主板首板小盘
+ 4 维分 ≥60 + 业绩雷区已过 → 当晚锁仓清单。"""

import pandas as pd

DABANG_FILTERS = {
    'price_max':     30.0,      # 单价 ≤30 元
    'mv_max_yi':     300.0,     # 流通市值 ≤300 亿
    'feng_time_max': '14:30',   # 封板时间 ≤14:30(去尾盘抢)
    'zhaban_max':    5,         # 炸板次数 ≤5
    'lianban_eq':    1,         # 只要首板
    'score_min':     60,        # 4 维综合 ≥60
}


def _col(df, keyword):
    """模糊匹配列名,返回第一个含 keyword 的列;无则返回 None。"""
    for c in df.columns:
        if keyword in str(c):
            return c
    return None


def _is_mainboard(code: str) -> bool:
    """主板 = 00xxxx + 60xxxx(科创北交所创业板都剔除)。"""
    c = str(code).zfill(6)
    return c.startswith('00') or c.startswith('60')


def _to_float(v):
    try:
        return float(v)
    except (TypeError, ValueError):
        return None


def compute_dabang_candidates(zt_pool_df, score_dict, top_n=5) -> list:
    """返回符合条件的涨停首板候选,按 4 维综合分降序。
      zt_pool_df: ak.stock_zt_pool_em 的 df,带'代码'/'最新价'/'流通市值'/'封板时间'/'炸板次数'/'连板数'。
      score_dict: {code6: {'composite': int, 'subscores': {...}, 'falsified': bool}}。
      失败/空 → 返回 []。"""
    if zt_pool_df is None or zt_pool_df.empty:
        return []

    code_col   = _col(zt_pool_df, '代码')
    name_col   = _col(zt_pool_df, '名称')
    price_col  = _col(zt_pool_df, '最新价') or _col(zt_pool_df, '价')
    mv_col     = _col(zt_pool_df, '流通市值') or _col(zt_pool_df, '市值')
    feng_col   = _col(zt_pool_df, '封板时间') or _col(zt_pool_df, '首次封板')
    zhaban_col = _col(zt_pool_df, '炸板')
    lianban_col= _col(zt_pool_df, '连板')

    if code_col is None:
        return []

    feng_max = DABANG_FILTERS['feng_time_max']
    out = []
    for _, row in zt_pool_df.iterrows():
        code = str(row[code_col]).zfill(6)
        if not _is_mainboard(code):
            continue

        price = _to_float(row[price_col]) if price_col else None
        if price is None or price > DABANG_FILTERS['price_max']:
            continue

        mv = _to_float(row[mv_col]) if mv_col else None
        if mv is not None:
            mv_yi = mv / 100_000_000
            if mv_yi > DABANG_FILTERS['mv_max_yi']:
                continue

        if feng_col:
            feng_v = str(row[feng_col])[:5] if pd.notna(row[feng_col]) else ''
            if feng_v and feng_v > feng_max:
                continue

        if zhaban_col:
            zb = _to_float(row[zhaban_col])
            if zb is not None and zb > DABANG_FILTERS['zhaban_max']:
                continue

        if lianban_col:
            lb = _to_float(row[lianban_col])
            if lb is None or int(lb) != DABANG_FILTERS['lianban_eq']:
                continue

        score = score_dict.get(code)
        if score is None:
            continue
        if score.get('falsified', False):
            continue
        composite = score.get('composite', 0)
        if composite < DABANG_FILTERS['score_min']:
            continue

        out.append({
            'code':      code,
            'name':      str(row[name_col]) if name_col else '',
            'price':     price,
            'mv_yi':     round(mv / 100_000_000, 1) if mv is not None else None,
            'feng_time': str(row[feng_col])[:5] if feng_col and pd.notna(row[feng_col]) else '',
            'zhaban':    int(_to_float(row[zhaban_col]) or 0) if zhaban_col else 0,
            'composite': composite,
            'subscores': score.get('subscores', {}),
        })

    out.sort(key=lambda r: r['composite'], reverse=True)
    return out[:top_n]
```

- [ ] **Step 2: 运行测试**

Run: `pytest 短线工具箱/tests/test_dabang_pool.py -v`
Expected: 8 个测试全 PASS

- [ ] **Step 3: Commit**

```bash
git add 短线工具箱/dabang_pool.py
git commit -m "feat(dabang): implement compute_dabang_candidates for v1.3"
```

---

## Task 7: 主程序 importlib — 接入两个新模块

**Files:**
- Modify: `筛选明日股票_v1.py:50-65`

- [ ] **Step 1: 读 v1.py 第 45-65 行确认当前 importlib 结构**

Run: `grep -n "_res_spec\|_sco_spec\|_bt_spec" 筛选明日股票_v1.py | head -10`
Expected: 形如:
```
47:_res_spec = _ilu.spec_from_file_location("akshare_resilient",
54:_sco_spec = _ilu.spec_from_file_location("scorer",
61:_bt_spec = _ilu.spec_from_file_location("backtest",
```

**注意命名约定:** v1.py 用 `_ilu` 作为 `importlib.util` 的别名,模块变量是 `_res_mod`/`_sco_mod`/`_bt_mod`(不是 `_ar_mod`)。新模块沿用 `_em_mod`/`_db_mod`。

- [ ] **Step 2: 在 backtest 的 importlib 块之后(约 line 65 那行 `for _bt_n` 之后),追加 emotion + dabang 两个模块的 importlib 代码**

打开 `筛选明日股票_v1.py`,在 line 65 那行 `for _bt_n in [...]: locals()[_bt_n] = getattr(_bt_mod, _bt_n)` 紧跟下方追加:

```python
# === v1.3 emotion + dabang 模块(importlib) ===
_em_spec = _ilu.spec_from_file_location("market_emotion",
    os.path.join(os.path.dirname(__file__), "短线工具箱", "market_emotion.py"))
_em_mod = _ilu.module_from_spec(_em_spec)
_em_spec.loader.exec_module(_em_mod)
for _en in ["compute_emotion_tier","POSITION_MULTIPLIER","EMOTION_THRESHOLDS"]: locals()[_en] = getattr(_em_mod, _en)

_db_spec = _ilu.spec_from_file_location("dabang_pool",
    os.path.join(os.path.dirname(__file__), "短线工具箱", "dabang_pool.py"))
_db_mod = _ilu.module_from_spec(_db_spec)
_db_spec.loader.exec_module(_db_mod)
for _dn in ["compute_dabang_candidates","DABANG_FILTERS"]: locals()[_dn] = getattr(_db_mod, _dn)
```

- [ ] **Step 3: 在 akshare_resilient 的 import 循环里追加 `fetch_zt_pool` + `fetch_market_activity`**

定位 v1.py 第 51 行:

```python
for _k in ["fetch_earnings","fetch_notice","fetch_lhb","fetch_lhb_inst","fetch_fund_flow_rank","fetch_hsgt","fetch_news"]: locals()[_k] = getattr(_res_mod, _k)
```

改为(只在末尾追加 2 个符号,别的别动):

```python
for _k in ["fetch_earnings","fetch_notice","fetch_lhb","fetch_lhb_inst","fetch_fund_flow_rank","fetch_hsgt","fetch_news","fetch_zt_pool","fetch_market_activity"]: locals()[_k] = getattr(_res_mod, _k)
```

- [ ] **Step 4: 验证语法 + import**

Run: `python -m py_compile 筛选明日股票_v1.py && python -c "import sys; sys.argv=['x','--mode','backtest','--days','0']; import 筛选明日股票_v1; print(筛选明日股票_v1.compute_emotion_tier, 筛选明日股票_v1.compute_dabang_candidates)"`
Expected: 打印两个函数对象,无异常。

- [ ] **Step 5: Commit**

```bash
git add 筛选明日股票_v1.py
git commit -m "feat(v1.3): wire market_emotion + dabang_pool via importlib"
```

---

## Task 8: 主程序 — 预拉缓存 + `_init_emotion_and_zt_pool`

**Files:**
- Modify: `筛选明日股票_v1.py`(在 `_load_lhb_inst_table` 之后,约 line 802)

- [ ] **Step 1: 在 `_load_lhb_inst_table` 函数之后追加 emotion + zt-pool 缓存**

打开 v1.py,找到 `return _LHB_INST_CACHE`(约 line 802)。紧跟其后追加:

```python
# === v1.3 涨停池 + 大盘情绪缓存 ===
_ZT_POOL_LOADED = False
_ZT_POOL_DF = None
_MARKET_ACT_DF = None
_EMOTION_TIER = {'tier': 'normal', 'multiplier': 1.0,
                 'zt_count': 0, 'lianban_count': 0, 'dt_count': 0,
                 'reason': '未初始化'}


def _init_emotion_and_zt_pool():
    """v1.3:预拉一次涨停池 + 市场活跃度,算出 emotion tier。
    任一失败 → 默认 tier=normal/multiplier=1.0,不影响主流程。"""
    global _ZT_POOL_LOADED, _ZT_POOL_DF, _MARKET_ACT_DF, _EMOTION_TIER
    if _ZT_POOL_LOADED:
        return _EMOTION_TIER
    _ZT_POOL_LOADED = True
    try:
        _ZT_POOL_DF = fetch_zt_pool()
        if _ZT_POOL_DF is None or _ZT_POOL_DF.empty:
            print('[预拉] 涨停池为空(可能非交易时段或休市)')
        else:
            print(f'[预拉] 涨停池 {len(_ZT_POOL_DF)} 条')
    except Exception as e:
        print(f'[预拉] 涨停池失败: {type(e).__name__}(打板池跳过)')
        _ZT_POOL_DF = None
    try:
        _MARKET_ACT_DF = fetch_market_activity()
        if _MARKET_ACT_DF is None or _MARKET_ACT_DF.empty:
            print('[预拉] 市场活跃度为空')
        else:
            print(f'[预拉] 市场活跃度 {len(_MARKET_ACT_DF)} 行')
    except Exception as e:
        print(f'[预拉] 市场活跃度失败: {type(e).__name__}(情绪门跳过)')
        _MARKET_ACT_DF = None
    _EMOTION_TIER = compute_emotion_tier(_ZT_POOL_DF, _MARKET_ACT_DF)
    print(f"[情绪] {_EMOTION_TIER['reason']} | 仓位乘数 {_EMOTION_TIER['multiplier']}")
    return _EMOTION_TIER
```

- [ ] **Step 2: 在主流程 LHB 预拉之后调一次 `_init_emotion_and_zt_pool()`**

Run: `grep -n "_load_lhb_table()\|_load_lhb_inst_table()" 筛选明日股票_v1.py` 找出主流程预拉行(不是函数定义、不是 `get_lhb_score` 内的调用)。

通常这些调用挂在 scan 模式主流程里(`if ARGS.mode != 'backtest'` 之后的 preload 段)。在最后一个预拉调用之后追加一行:

```python
_init_emotion_and_zt_pool()
```

(若 grep 没找到独立的预拉行,说明 LHB 是 lazy 在 `get_lhb_score` 内首次调用时拉的。此时把 `_init_emotion_and_zt_pool()` 放在 `if ARGS.mode != 'backtest'` 进入 scan 后、`scan_pool` 之前的位置——具体行号实施时定。)

- [ ] **Step 3: 验证 import + 函数可调用(用 backtest --days 0 模式跳过真实拉数)**

Run: `python 筛选明日股票_v1.py --mode backtest --days 0`
Expected: 不报错退出(回测段会因 days=0 直接返回 None;emotion 段不会跑因为是 backtest 模式)。

- [ ] **Step 4: Commit**

```bash
git add 筛选明日股票_v1.py
git commit -m "feat(v1.3): add _init_emotion_and_zt_pool preload"
```

---

## Task 9: 主程序 — 仓位套 emotion multiplier

**Files:**
- Modify: `筛选明日股票_v1.py:1769`(TOP3 仓位分配段)

- [ ] **Step 1: 修改 TOP3 仓位计算**

打开 v1.py,定位第 1769 行附近:

```python
for r in top3:
    r['position_pct'] = dynamic_position_pct(r['composite'], market_info['level'])
```

改为(只新增 emotion 乘法,保留原 `dynamic_position_pct`):

```python
for r in top3:
    base_pct = dynamic_position_pct(r['composite'], market_info['level'])
    r['position_pct'] = round(base_pct * _EMOTION_TIER['multiplier'], 1)
    r['position_base'] = base_pct
    r['position_emotion'] = _EMOTION_TIER['tier']
```

- [ ] **Step 2: 验证语法 + 仍能跑 backtest 模式**

Run: `python -m py_compile 筛选明日股票_v1.py && python 筛选明日股票_v1.py --mode backtest --days 0`
Expected: 编译过,backtest 模式正常退出 0。

- [ ] **Step 3: Commit**

```bash
git add 筛选明日股票_v1.py
git commit -m "feat(v1.3): apply emotion multiplier to TOP3 position_pct"
```

---

## Task 10: 打板池打印函数 — 加到 `backtest.py`

**Files:**
- Modify: `短线工具箱/backtest.py`

- [ ] **Step 1: 在 `backtest.py` 末尾追加 `print_dabang_pool_report`**

打开 `短线工具箱/backtest.py`,在文件末尾追加:

```python
def print_dabang_pool_report(candidates, emotion_tier):
    """打印涨停板候选池 CLI 报告(v1.3)。candidates 来自 compute_dabang_candidates。"""
    print("\n" + "=" * 70)
    print(f"  [打板池] 首板小盘候选(主板 + 4维≥60 + 业绩雷区已过)")
    print(f"  [情绪] {emotion_tier['reason']} | 仓位乘数 {emotion_tier['multiplier']}")
    print("=" * 70)
    if not candidates:
        print("  (空)今日无符合条件的首板小盘 — 涨停池可能为空或全被过滤")
        print("=" * 70)
        return
    print(f"  {'代码':<8}{'名称':<10}{'综合':<6}{'价格':<8}{'流通(亿)':<10}{'封板':<8}{'炸板':<6}")
    print("  " + "-" * 60)
    for c in candidates:
        print(f"  {c['code']:<8}{c['name']:<10}{c['composite']:<6}"
              f"{c['price']:<8.2f}{(c['mv_yi'] or 0):<10.1f}"
              f"{c['feng_time']:<8}{c['zhaban']:<6}")
    print("=" * 70)
```

- [ ] **Step 2: 验证语法**

Run: `python -m py_compile 短线工具箱/backtest.py`
Expected: 退出码 0

- [ ] **Step 3: 在 v1.py 第 65 行附近的 backtest importlib 符号清单里追加 `print_dabang_pool_report`**

找到 v1.py 中类似 `for _bt_n in ["print_backtest_report","dim_name_cn","win_diff_cn","print_attribution_report"]:` 的行(约 line 65),把列表改为:

```python
for _bt_n in ["print_backtest_report","dim_name_cn","win_diff_cn","print_attribution_report","print_dabang_pool_report"]: locals()[_bt_n] = getattr(_bt_mod, _bt_n)
```

- [ ] **Step 4: 验证 import**

Run: `python -c "import sys; sys.argv=['x','--mode','backtest','--days','0']; import 筛选明日股票_v1; print(筛选明日股票_v1.print_dabang_pool_report)"`
Expected: 打印函数对象。

- [ ] **Step 5: Commit**

```bash
git add 短线工具箱/backtest.py 筛选明日股票_v1.py
git commit -m "feat(v1.3): add print_dabang_pool_report"
```

---

## Task 11: 主程序 — 打板池 CLI 输出 + HTML section

**Files:**
- Modify: `筛选明日股票_v1.py`(约 line 2115-2128,full 模式输出段)

- [ ] **Step 1: 读 v1.py 第 2107-2130 行**

Run: `grep -n "backtest_report = backtest_score_history\|html_path = generate_html_report" 筛选明日股票_v1.py`
确认行号在 2110-2128 之间。

- [ ] **Step 2: 在 `print_attribution_report(attr_report)` 之后、`generate_html_report` 之前插入打板池计算 + 打印**

定位 v1.py 第 2122 行附近的 `print_attribution_report(attr_report)`。在其紧后追加:

```python
    # === v1.3 打板候选池 ===
    score_dict_for_dabang = {
        r['code']: {
            'composite': r['composite'],
            'subscores': r['subscores'],
            'falsified': bool(r.get('falsified_signals') or compute_falsification_signals(r)),
        }
        for r in results
    }
    dabang_candidates = compute_dabang_candidates(
        _ZT_POOL_DF, score_dict_for_dabang, top_n=5,
    )
    print_dabang_pool_report(dabang_candidates, _EMOTION_TIER)
```

**Note:** `falsified` 这里采宽口径——只要 `compute_falsification_signals` 返回非空就算"踩过雷"——这与 spec §2.3 一致(打板池只放完全无雷的)。若 v1.py 的 results 字典里已经有 `falsified_signals` 字段,优先用它;没有就现算。

- [ ] **Step 3: 把 `dabang_candidates` 传给 `generate_html_report`**

定位 v1.py 第 2128 行:

```python
html_path = generate_html_report(results, top3, market_info, backtest_report, recent_recos, attr_report)
```

改为:

```python
html_path = generate_html_report(
    results, top3, market_info, backtest_report, recent_recos, attr_report,
    dabang=dabang_candidates, emotion=_EMOTION_TIER,
)
```

- [ ] **Step 4: 改 `generate_html_report` 签名 + 加 dabang/emotion HTML section**

定位 v1.py 第 1239 行 `def generate_html_report(...)`,把签名改为:

```python
def generate_html_report(results, top3, market_info, backtest=None, history_recos=None, attribution=None, dabang=None, emotion=None):
```

在 `bt_html`/`attr_html` 准备段之后、组装总 html 之前,追加:

```python
    # === v1.3 大盘情绪 + 打板池 HTML ===
    emotion_html = ""
    if emotion:
        emotion_html = f"""
<div class="section">
  <h2>[大盘情绪] {emotion['tier']}(仓位乘数 {emotion['multiplier']})</h2>
  <p>{emotion['reason']}</p>
</div>"""

    dabang_html = ""
    if dabang:
        rows = ""
        for c in dabang:
            rows += (f"<tr><td>{c['code']}</td><td>{c['name']}</td>"
                     f"<td><b>{c['composite']}</b></td>"
                     f"<td>{c['price']:.2f}</td>"
                     f"<td>{(c['mv_yi'] or 0):.1f}亿</td>"
                     f"<td>{c['feng_time']}</td>"
                     f"<td>{c['zhaban']}</td></tr>")
        dabang_html = f"""
<div class="section">
  <h2>[打板池] 首板小盘候选(主板 + 4维≥60 + 雷区已过)</h2>
  <table>
    <tr><th>代码</th><th>名称</th><th>综合</th><th>价格</th><th>流通市值</th><th>封板</th><th>炸板</th></tr>
    {rows}
  </table>
</div>"""
    elif emotion:
        dabang_html = """
<div class="section">
  <h2>[打板池] 首板小盘候选</h2>
  <p>(空)今日无符合条件的首板小盘。</p>
</div>"""
```

然后,在 `html = f"""<!DOCTYPE html>...` 那个 f-string 模板里,找到合适位置(建议在 `{bt_html}` 之后)插入 `{emotion_html}{dabang_html}`:

```python
{bt_html}
{emotion_html}
{dabang_html}
{attr_html}
```

(具体位置以模板现状为准,挨着 bt_html 放即可。)

- [ ] **Step 5: 验证语法 + 用 backtest 模式跑(不会触发 dabang 段)**

Run: `python -m py_compile 筛选明日股票_v1.py && python 筛选明日股票_v1.py --mode backtest --days 0`
Expected: 编译过,退出 0。

- [ ] **Step 6: 跑两个回归测试套(不能因为加新参数破坏旧测试)**

Run: `pytest 短线工具箱/tests/test_scorer.py 短线工具箱/tests/test_market_emotion.py 短线工具箱/tests/test_dabang_pool.py -v`
Expected: scorer 的旧测试 + 新 4+8=12 个新测试全 PASS。

(已知 `test_p1_lead_signal.py` 6 个测试因 conftest.py 路径补丁问题失败,与本次改动无关——见 spec §3.2。)

- [ ] **Step 7: Commit**

```bash
git add 筛选明日股票_v1.py
git commit -m "feat(v1.3): integrate dabang pool + emotion into CLI/HTML output"
```

---

## Task 12: README 文档更新

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 找 README 中证伪门或先行信号段落位置**

Run: `grep -n "证伪门\|先行信号升级\|## " README.md | head -30`
预期能看到 §证伪门 / §先行信号升级 之类锚点。

- [ ] **Step 2: 在 §先行信号升级 之后追加 §涨停板候选池 + 情绪门**

打开 `README.md`,在 §先行信号升级(P1)章节之后追加:

```markdown
## 涨停板候选池 + 大盘情绪门(v1.3 · 2026-06-14 加)

两个新机制,放在 4 维评分之上做"二次过滤 + 仓位调节":

### 大盘情绪门(仓位乘数)

每日预拉涨停股池 + 市场活跃度,分 4 档:

| 档位 | 触发条件 | 仓位乘数 |
|------|----------|----------|
| hot  | 连板≥30 且 涨停≥80 | 1.0 |
| normal | 其余 | 0.8 |
| cool | 连板<10 | 0.5 |
| cold | 涨停<30 | 0.2 |

优先级:cold > cool > hot > normal。TOP3 的 `position_pct` 自动乘上对应乘数。
数据拉取失败 → tier='normal'/multiplier=1.0,不影响 v1.2 行为。

### 涨停板候选池(首板小盘)

每日扫描后,从涨停股池筛主板首板小盘候选:

- 主板 00xxxx / 60xxxx
- 单价 ≤30 元
- 流通市值 ≤300 亿
- 封板时间 ≤14:30
- 炸板次数 ≤5
- 连板数 =1(只要首板)
- 4 维综合分 ≥60
- 业绩雷区已过(`compute_falsification_signals` 为空)

按综合分降序输出 TOP5,落到 CLI 报告 + HTML 报告尾部。
涨停池为空(非交易时段/休市) → 跳过,不报错。
```

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs(readme): add v1.3 dabang pool + emotion gate section"
```

---

## Task 13: AGENT_LOG 追加 + 全量自检

**Files:**
- Modify: `docs/AGENT_LOG.md`(若不存在,跳过该段;不影响主流程)

- [ ] **Step 1: 检查 AGENT_LOG.md 是否存在**

Run: `test -f docs/AGENT_LOG.md && echo exists || echo missing`

如果存在(echo exists),执行 Step 2;否则跳到 Step 3。

- [ ] **Step 2: 追加历史事件条目**

打开 `docs/AGENT_LOG.md`,在文件末尾追加(注意保持文件原有格式):

```markdown
## 2026-06-14 — v1.3 涨停板候选池 + 大盘情绪门

- 新增 `短线工具箱/market_emotion.py` + `短线工具箱/dabang_pool.py`(各带 4/8 单测,全过)
- `akshare_resilient.py` 追加 `fetch_zt_pool` / `fetch_market_activity` 2 包装
- `筛选明日股票_v1.py` TOP3 仓位套 emotion multiplier;主流程尾部追打板池一节
- README 新增 §涨停板候选池 + 情绪门
- 失败路径全降级到 v1.2 行为,稳准 > 效率
```

- [ ] **Step 3: 全量自检**

依次跑:

```bash
python -m py_compile 筛选明日股票_v1.py
python -c "import sys; sys.argv=['x','--mode','backtest','--days','0']; import 筛选明日股票_v1; print('import ok')"
pytest 短线工具箱/tests/test_market_emotion.py -v
pytest 短线工具箱/tests/test_dabang_pool.py -v
pytest 短线工具箱/tests/test_scorer.py -v
python daily.py --help
```

Expected:
- 编译 + import: 无报错,打印 `import ok`
- test_market_emotion: 4 passed
- test_dabang_pool: 8 passed
- test_scorer: 全 PASS(13 旧用例,不能回归)
- daily.py --help: 退出 0

(已知 `test_p1_lead_signal.py` 因 conftest.py 路径补丁不完整,6 个用例失败,与本次改动无关,见 spec §3.2。)

- [ ] **Step 4: Commit(如果改了 AGENT_LOG.md)**

```bash
git add docs/AGENT_LOG.md
git commit -m "docs(agent-log): record v1.3 dabang + emotion landing"
```

(若 AGENT_LOG.md 不存在,无需 commit,直接结束。)

---

## 实施完成验收

完成 Task 13 后,本 plan 进入"等用户拍板 push"状态。

- 不要自动 push(参考 spec §7 业务约束:AI 不擅自 push)
- 用户跑 5-10 个交易日 → 用 `python daily.py --backtest` 看 emotion tier 命中率 + dabang 池命中率
- 若 cold/cool 时段 dabang 池命中率 <30% → 用户决定要不要调阈值

回滚预案(参考 spec §6.2):

```bash
# 轻度回滚:删 emotion + dabang 调用,留模块代码
git revert <task-9-commit>
git revert <task-11-commit>

# 完全回滚:
git revert HEAD~13..HEAD   # 一次 revert 整组
```
