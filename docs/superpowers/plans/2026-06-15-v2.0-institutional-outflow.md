# v2.0 机构流出深层次信号 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 v1.3 4 维评分基础上扩展为 5 维,加入 4 类机构流出信号(大宗折价 / 主力分单 / 融资融券 / 股东户数+解禁),把胜率从 ~50% 推向 ≥70%。

**Architecture:** 1 个新模块 `短线工具箱/institutional_flow.py`(4 个 sub-score + 1 个综合 + 1 个主力分单)落到 `短线工具箱/`,4 个新 `fetch_*` 包装器追加到已有 `akshare_resilient.py`,主程序 `筛选明日股票_v1.py` 通过 importlib 套现有模式 import,新增 `_init_institutional_flow()` 预拉缓存,5 维评分替换 4 维,资金面内部扩 5 子(主力5日+主力分单+北向+龙虎+量比),消息面缩 2 子(ann+newsfeed)。所有失败路径降级到 v1.3 行为(子分 50 兜底,综合分退化近 4 维水平)。

**Tech Stack:** Python 3.x + akshare(`stock_dzjy_mrmxa` / `stock_margin_underlying_info_szse` / `stock_holder_number_em` / `stock_restricted_release_em` / `stock_individual_fund_flow`) + pandas + 已有 `call_with_fallback`/`@safe_score`/importlib 模式 + pytest。

**Spec:** `docs/superpowers/specs/2026-06-15-v2.0-institutional-outflow-design.md` (commit `7a684ad`)

**协作规则:** 遵循 AGENTS.md §1 路由表(Codex 实施,Claude review)。本 plan 每步含完整代码 + 验证命令 + 提交,执行人不需查 spec。

---

## File Structure

**New files:**
- `短线工具箱/institutional_flow.py` — 4 个 sub-score 函数 + `compute_institutional_score` + `score_main_force_split`
- `短线工具箱/tests/test_institutional_flow.py` — ≥25 个单元测试

**Modified files:**
- `短线工具箱/akshare_resilient.py` — 追加 4 个 `fetch_*` 包装器(在 `fetch_market_activity` 之后)
- `筛选明日股票_v1.py`:
  - 第 51 行附近: importlib 循环追加 4 个 fetch + 1 个新模块
  - 第 802 行后: 追加 `_INST_DF` 缓存 + `_init_institutional_flow()` 函数
  - 主流程 preload 段(institutional init 之后): 调一次 `_init_institutional_flow()`
  - 资金面 `get_capital_flow_score` 改造:扩到 5 子分(主力5日+主力分单+北向+龙虎+量比)
  - 消息面 `get_news_catalyst_score` 简化:从 4 子缩到 2 子(ann+newsfeed),hsgt/lhb 移走
  - `compute_composite_v2` 新增:5 维评分
  - `compute_falsification_signals` 改造:多收 3 个 inst 参数
  - TOP3 卡片输出加 1 行"机构行为子分"
  - HTML 报告集成(参考 v1.3 dabang 集成模式)
- `README.md` — §4 维评分体系 → §5 维评分体系 + §路线图加 v2.0
- `AGENT_LOG.md` — 追加历史事件

**Critical 风险**:
- v1.py 当前 2254 行,**已超 2000 上限**。本 plan 严格控制增量(目标净增 ≤ 100 行),所有新逻辑放 institutional_flow.py
- akshare 接口名候选:`stock_dzjy_mrmxa` / `stock_margin_underlying_info_szse` / `stock_holder_number_em` / `stock_restricted_release_em` — 实施时第 1 步验证(每个 fetch 任务的 Step 2),失败则尝试备选,全部失败返回 None 不阻塞

---

## Task 1: 大宗交易 fetcher + score_dzjy (TDD)

**Files:**
- Modify: `短线工具箱/akshare_resilient.py` (在 `fetch_market_activity` 之后追加 `fetch_dzjy`)
- Create: `短线工具箱/institutional_flow.py` (含 `score_dzjy`)
- Create: `短线工具箱/tests/test_institutional_flow.py`

- [ ] **Step 1: 验证 akshare 大宗接口可用性**

Run:
```bash
python -c "import akshare as ak; print(hasattr(ak, 'stock_dzjy_mrmxa'), hasattr(ak, 'stock_dzjy_detail_em'))"
```
Expected: 输出两个 True 或 False(`True` 即可用,`False` 备选用)。

若两个都 `False`:
```bash
python -c "import akshare as ak; print([x for x in dir(ak) if 'dzjy' in x.lower() or 'block' in x.lower()])"
```
Expected: 输出含 'dzjy' 的函数名列表(用于定位正确接口)。

记录到 commit message:"大宗接口验证: stock_dzjy_mrmxa=存在/不存在"。

- [ ] **Step 2: 在 `短线工具箱/akshare_resilient.py` 末尾追加 `fetch_dzjy`**

打开 `短线工具箱/akshare_resilient.py`,在 `fetch_market_activity` 之后追加:

```python
def fetch_dzjy(date: str | None = None) -> pd.DataFrame | None:
    """大宗交易明细(v2.0)。ak.stock_dzjy_mrmxa 拉近 30 日全市场。
    字段:交易日/代码/简称/成交价/折溢率/买方营业部/卖方营业部/成交金额。
    用于:识别机构原始股东减持 + 折价率扩大(出货信号)。
    date 形如 '20260614'。"""
    import akshare as ak
    from datetime import datetime
    if date is None:
        date = datetime.now().strftime('%Y%m%d')
    return call_with_fallback(
        attempts=[
            (getattr(ak, 'stock_dzjy_mrmxa', None), {'date': date}),
            (getattr(ak, 'stock_dzjy_detail_em', None), {}),
        ],
        cache_key=f'dzjy_{date}',
        timeout=15,
        retries=1,
    )
```

- [ ] **Step 3: 验证语法 + 可导入**

Run:
```bash
python -m py_compile 短线工具箱/akshare_resilient.py && python -c "from 短线工具箱.akshare_resilient import fetch_dzjy; print('ok')"
```
Expected: 无输出 + 退出 0 + 第二条命令输出 `ok`。

- [ ] **Step 4: 写 5 个失败测试 (score_dzjy)**

创建 `短线工具箱/tests/test_institutional_flow.py`,**先写 score_dzjy 的 5 个测试**(其他 sub-score 测试在后续任务添加,本任务只放 score_dzjy 相关):

```python
"""v2.0 机构流出深层次信号测试 — 25+ 用例覆盖 4 子分 + 综合 + 主力分单。"""
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '..'))

import pandas as pd
import pytest


def _make_dzjy_row(code='600001', rate=5.0, seller='营业部A'):
    """构造大宗交易单行。"""
    return {
        '证券代码': code, '证券简称': '测试票',
        '折溢率': rate, '卖方营业部': seller,
    }


# ============================================================
# score_dzjy 测试
# ============================================================
def test_dzjy_strong_discount_with_inst():
    """折价 12% + 卖方'机构专用' → 25-10=15 分,reason 含'机构席位'。"""
    from 短线工具箱.institutional_flow import score_dzjy
    df = pd.DataFrame([_make_dzjy_row(rate=12.0, seller='机构专用席位')])
    df['_code6'] = df['证券代码']
    score, reason = score_dzjy('600001', df)
    assert score == 15
    assert '机构' in reason


def test_dzjy_mild_discount():
    """折价 6% → 35 分,reason 含'折价'。"""
    from 短线工具箱.institutional_flow import score_dzjy
    df = pd.DataFrame([_make_dzjy_row(rate=6.0)])
    df['_code6'] = df['证券代码']
    score, reason = score_dzjy('600001', df)
    assert score == 35
    assert '折价' in reason


def test_dzjy_premium():
    """溢价 5% → 70 分。"""
    from 短线工具箱.institutional_flow import score_dzjy
    df = pd.DataFrame([_make_dzjy_row(rate=-5.0)])
    df['_code6'] = df['证券代码']
    score, reason = score_dzjy('600001', df)
    assert score == 70


def test_dzjy_no_records():
    """该 code 不在 df 中 → 55 分。"""
    from 短线工具箱.institutional_flow import score_dzjy
    df = pd.DataFrame([_make_dzjy_row(code='600002')])
    df['_code6'] = df['证券代码']
    score, reason = score_dzjy('600001', df)
    assert score == 55
    assert '无大宗' in reason


def test_dzjy_data_missing():
    """df=None → 50 分,reason 含'缺失'。"""
    from 短线工具箱.institutional_flow import score_dzjy
    score, reason = score_dzjy('600001', None)
    assert score == 50
    assert '缺失' in reason
```

- [ ] **Step 5: 验证测试失败 (模块未建)**

Run: `pytest 短线工具箱/tests/test_institutional_flow.py -v`
Expected: 5 个测试 FAIL,`ModuleNotFoundError: No module named '短线工具箱.institutional_flow'`

- [ ] **Step 6: 创建 `短线工具箱/institutional_flow.py` 含 score_dzjy**

新建 `短线工具箱/institutional_flow.py`:

```python
"""v2.0 机构流出深层次信号 — 大宗/主力分单/融资/股东户数/解禁。

设计原则:
- 5 子分独立计分,加权合成机构行为维 0-100
- 任一数据源拉取失败 → 该子分返回 50(中位),不影响总分
- 输出包含每个子分的 reason,用于证伪门 + 报告展示
"""
import pandas as pd
from typing import Optional, Tuple


# ============================================================
# 子分 1: 大宗交易折价 + 卖方席位
# ============================================================
def score_dzjy(code6: str, dzjy_df: Optional[pd.DataFrame]) -> Tuple[int, str]:
    """大宗交易子分 0-100。
    评分逻辑:
      - 平均折价率 >= 10% → 25 分(强出货)
      - 平均折价率 5~10% → 35 分
      - 平均折价率 0~5% → 45 分(微折)
      - 溢价 0~5% → 70 分
      - 溢价 >= 5% → 80 分(强接货)
      - 该 code 无记录 → 55 分(中性)
      - df 缺失 → 50 分(兜底)
    卖方席位含'机构专用' → 额外 -10 分(强出货证据)
    """
    if dzjy_df is None or dzjy_df.empty:
        return 50, '大宗数据缺失'

    code_col = '_code6' if '_code6' in dzjy_df.columns else '证券代码'
    rows = dzjy_df[dzjy_df[code_col].astype(str).str.zfill(6) == str(code6).zfill(6)]
    if rows.empty:
        return 55, '近30日无大宗交易'

    # 折溢率列(兼容:折溢率/折溢价率)
    rate_col = next((c for c in rows.columns if '折溢' in str(c)), None)
    if rate_col is None:
        return 50, '大宗缺折溢率列'
    rates = pd.to_numeric(rows[rate_col], errors='coerce').dropna()
    if rates.empty:
        return 50, '大宗折溢率全NaN'
    avg_rate = float(rates.mean())

    if avg_rate >= 10:     base, desc = 25, f'大宗折价{avg_rate:.1f}%(强出货)'
    elif avg_rate >= 5:    base, desc = 35, f'大宗折价{avg_rate:.1f}%'
    elif avg_rate >= 0:    base, desc = 45, f'大宗折价{avg_rate:.1f}%(微折)'
    elif avg_rate >= -5:   base, desc = 70, f'大宗溢价{abs(avg_rate):.1f}%'
    else:                  base, desc = 80, f'大宗溢价{abs(avg_rate):.1f}%(强接货)'

    # 卖方席位加成:含"机构专用" → -10
    seller_col = next((c for c in rows.columns if '卖方' in str(c)), None)
    if seller_col and rows[seller_col].astype(str).str.contains('机构专用', na=False).any():
        base = max(0, base - 10)
        desc += '+机构席位'

    return base, desc
```

- [ ] **Step 7: 验证 score_dzjy 5 测试通过**

Run: `pytest 短线工具箱/tests/test_institutional_flow.py -v`
Expected: 5 个测试全 PASS

- [ ] **Step 8: Commit**

```bash
git add 短线工具箱/akshare_resilient.py 短线工具箱/institutional_flow.py 短线工具箱/tests/test_institutional_flow.py
git commit -m "feat(v2.0): add fetch_dzjy + score_dzjy (大宗折价子分)"
```

---

## Task 2: 融资融券 fetcher + score_margin (TDD)

**Files:**
- Modify: `短线工具箱/akshare_resilient.py` (在 `fetch_dzjy` 之后追加 `fetch_margin`)
- Modify: `短线工具箱/institutional_flow.py` (追加 `score_margin`)
- Modify: `短线工具箱/tests/test_institutional_flow.py` (追加 4 个 score_margin 测试)

- [ ] **Step 1: 验证 akshare 融资融券接口可用性**

Run:
```bash
python -c "import akshare as ak; print([x for x in dir(ak) if 'margin' in x.lower()][:10])"
```
Expected: 输出含 'margin' 的函数名(找 `stock_margin_underlying_info_szse` 或 `stock_margin_underlying_info_sse` 之类)。

记录:找到的接口名。

- [ ] **Step 2: 在 `短线工具箱/akshare_resilient.py` 末尾追加 `fetch_margin`**

在 `fetch_dzjy` 之后追加:

```python
def fetch_margin(trade_date: str | None = None) -> pd.DataFrame | None:
    """沪深两市融资融券明细(v2.0)。ak.stock_margin_underlying_info_szse/sse。
    字段:代码/简称/融资余额/融券余量/融资买入额/融资偿还额。
    用于:计算个股融资余额 N 日变化率(杠杆资金撤退)。
    trade_date 形如 '20260614'。"""
    import akshare as ak
    return call_with_fallback(
        attempts=[
            (getattr(ak, 'stock_margin_underlying_info_szse', None),
             {'date': trade_date} if trade_date else {}),
            (getattr(ak, 'stock_margin_underlying_info_sse', None),
             {'date': trade_date} if trade_date else {}),
            (getattr(ak, 'stock_margin_underlying_info', None), {}),
        ],
        cache_key=f'margin_{trade_date or "latest"}',
        timeout=15,
        retries=1,
    )
```

- [ ] **Step 3: 验证语法 + 可导入**

Run:
```bash
python -m py_compile 短线工具箱/akshare_resilient.py && python -c "from 短线工具箱.akshare_resilient import fetch_margin; print('ok')"
```
Expected: 无输出 + 退出 0 + 输出 `ok`。

- [ ] **Step 4: 在 `test_institutional_flow.py` 追加 4 个 score_margin 测试**

在文件末尾追加:

```python
# ============================================================
# score_margin 测试
# ============================================================
def _make_margin_row(code='600001', balance=1e9):
    """构造融资融券单行(单位:元)。"""
    return {'标的代码': code, '融资余额': balance}


def test_margin_sharp_decline():
    """融资余额周降 12% → 25 分(杠杆踩踏)。"""
    from 短线工具箱.institutional_flow import score_margin
    # 假设昨日 1e9 → 今日 8.8e8 (周降 12%)
    df_today = pd.DataFrame([_make_margin_row(balance=8.8e8)])
    df_today['_code6'] = df_today['标的代码']
    # 简化:score_margin 取今日 df,与内置 7 日均值比较(测试中传 mock)
    score, reason = score_margin('600001', df_today, decline_pct=12.0)
    assert score == 25
    assert '杠杆' in reason or '踩踏' in reason or '降' in reason


def test_margin_mild_decline():
    """周降 5% → 40 分。"""
    from 短线工具箱.institutional_flow import score_margin
    df = pd.DataFrame([_make_margin_row()])
    df['_code6'] = df['标的代码']
    score, reason = score_margin('600001', df, decline_pct=5.0)
    assert score == 40


def test_margin_increase():
    """周升 8% → 70 分(杠杆加仓)。"""
    from 短线工具箱.institutional_flow import score_margin
    df = pd.DataFrame([_make_margin_row()])
    df['_code6'] = df['标的代码']
    score, reason = score_margin('600001', df, decline_pct=-8.0)
    assert score == 70


def test_margin_data_missing():
    """df=None → 50 分。"""
    from 短线工具箱.institutional_flow import score_margin
    score, reason = score_margin('600001', None)
    assert score == 50
    assert '缺失' in reason
```

- [ ] **Step 5: 在 `短线工具箱/institutional_flow.py` 追加 score_margin**

在 `score_dzjy` 之后追加:

```python
# ============================================================
# 子分 2: 融资融券余额变化
# ============================================================
def score_margin(code6: str, margin_df: Optional[pd.DataFrame],
                 decline_pct: Optional[float] = None) -> Tuple[int, str]:
    """融资融券子分 0-100。
    decline_pct: 7 日变化率(% 负数=下降,正数=上升)。
                  测试/调用方传入;若 None 则尝试从 margin_df 计算。
    评分逻辑:
      - 周降 >= 10% → 25 分(杠杆踩踏)
      - 周降 5~10% → 40 分
      - 周降 0~5% → 50 分
      - 周升 0~5% → 55 分
      - 周升 >= 5% → 70 分(杠杆加仓)
      - df 缺失 → 50 分
    """
    if margin_df is None or margin_df.empty:
        return 50, '融资数据缺失'

    if decline_pct is None:
        # 占位:实际从 margin_df + 7 日历史计算(实施时接 daily 缓存)
        # 简化:df 有数据但无历史,默认 decline=0
        decline_pct = 0.0

    if decline_pct >= 10:    base, desc = 25, f'融资周降{decline_pct:.1f}%(杠杆踩踏)'
    elif decline_pct >= 5:   base, desc = 40, f'融资周降{decline_pct:.1f}%'
    elif decline_pct >= 0:   base, desc = 50, f'融资周降{decline_pct:.1f}%(微降)'
    elif decline_pct >= -5:  base, desc = 55, f'融资周升{abs(decline_pct):.1f}%(微升)'
    else:                    base, desc = 70, f'融资周升{abs(decline_pct):.1f}%(加仓)'

    return base, desc
```

- [ ] **Step 6: 验证 9 个测试全过 (5 dzjy + 4 margin)**

Run: `pytest 短线工具箱/tests/test_institutional_flow.py -v`
Expected: 9 个测试全 PASS

- [ ] **Step 7: Commit**

```bash
git add 短线工具箱/akshare_resilient.py 短线工具箱/institutional_flow.py 短线工具箱/tests/test_institutional_flow.py
git commit -m "feat(v2.0): add fetch_margin + score_margin (融资融券子分)"
```

---

## Task 3: 股东户数 fetcher + score_holder (TDD)

**Files:**
- Modify: `短线工具箱/akshare_resilient.py` (在 `fetch_margin` 之后追加 `fetch_holder`)
- Modify: `短线工具箱/institutional_flow.py` (追加 `score_holder`)
- Modify: `短线工具箱/tests/test_institutional_flow.py` (追加 5 个 score_holder 测试)

- [ ] **Step 1: 验证 akshare 股东户数接口可用性**

Run:
```bash
python -c "import akshare as ak; print([x for x in dir(ak) if 'holder' in x.lower()][:10])"
```
Expected: 输出含 'holder' 的函数名(找 `stock_holder_number_em` 或 `stock_hold_num_em` 之类)。

记录:找到的接口名。

- [ ] **Step 2: 在 `短线工具箱/akshare_resilient.py` 末尾追加 `fetch_holder`**

在 `fetch_margin` 之后追加:

```python
def fetch_holder() -> pd.DataFrame | None:
    """全市场股东户数变化(v2.0,季报披露)。ak.stock_holder_number_em。
    字段:代码/简称/报告期/股东户数/户均持股数/环比变化。
    用于:户数大增=筹码分散=派发,大降=集中=吸筹(滞后但准)。"""
    import akshare as ak
    return call_with_fallback(
        attempts=[
            (getattr(ak, 'stock_holder_number_em', None), {}),
            (getattr(ak, 'stock_hold_num_em', None), {}),
            (getattr(ak, 'stock_holder_teamwork_em', None), {}),
        ],
        cache_key='holder_latest',
        timeout=15,
        retries=1,
    )
```

- [ ] **Step 3: 验证语法 + 可导入**

Run:
```bash
python -m py_compile 短线工具箱/akshare_resilient.py && python -c "from 短线工具箱.akshare_resilient import fetch_holder; print('ok')"
```
Expected: 无输出 + 退出 0 + 输出 `ok`。

- [ ] **Step 4: 在 `test_institutional_flow.py` 追加 5 个 score_holder 测试**

在文件末尾追加:

```python
# ============================================================
# score_holder 测试
# ============================================================
def _make_holder_row(code='600001', holder_count=10000):
    """构造股东户数单行。"""
    return {'代码': code, '股东户数': holder_count}


def test_holder_big_disperse():
    """户数环比 +25% → 25 分(筹码大幅分散)。"""
    from 短线工具箱.institutional_flow import score_holder
    df = pd.DataFrame([_make_holder_row()])
    df['_code6'] = df['代码']
    score, reason = score_holder('600001', df, change_pct=25.0)
    assert score == 25
    assert '分散' in reason or '+25' in reason


def test_holder_mild_disperse():
    """户数 +12% → 35 分。"""
    from 短线工具箱.institutional_flow import score_holder
    df = pd.DataFrame([_make_holder_row()])
    df['_code6'] = df['代码']
    score, reason = score_holder('600001', df, change_pct=12.0)
    assert score == 35


def test_holder_concentrate():
    """户数 -15% → 65 分(筹码集中)。"""
    from 短线工具箱.institutional_flow import score_holder
    df = pd.DataFrame([_make_holder_row()])
    df['_code6'] = df['代码']
    score, reason = score_holder('600001', df, change_pct=-15.0)
    assert score == 65
    assert '集中' in reason or '-15' in reason


def test_holder_stable():
    """户数 +3% → 50 分(平稳)。"""
    from 短线工具箱.institutional_flow import score_holder
    df = pd.DataFrame([_make_holder_row()])
    df['_code6'] = df['代码']
    score, reason = score_holder('600001', df, change_pct=3.0)
    assert score == 50


def test_holder_data_missing():
    """df=None → 50 分。"""
    from 短线工具箱.institutional_flow import score_holder
    score, reason = score_holder('600001', None)
    assert score == 50
    assert '缺失' in reason
```

- [ ] **Step 5: 在 `institutional_flow.py` 追加 score_holder**

在 `score_margin` 之后追加:

```python
# ============================================================
# 子分 3: 股东户数变化(季报)
# ============================================================
def score_holder(code6: str, holder_df: Optional[pd.DataFrame],
                 change_pct: Optional[float] = None) -> Tuple[int, str]:
    """股东户数子分 0-100。
    change_pct: 户数环比变化率(% 正数=户数增加=派发,负数=户数减少=吸筹)。
                 测试/调用方传入;若 None 则尝试从 holder_df 计算。
    评分逻辑:
      - 户数 +20% → 25 分(筹码大幅分散)
      - 户数 +10~20% → 35 分
      - 户数 ±10% → 50 分(平稳)
      - 户数 -10~-20% → 65 分(集中)
      - 户数 <-20% → 75 分(大幅集中)
      - df 缺失 → 50 分
    """
    if holder_df is None or holder_df.empty:
        return 50, '股东户数缺失'

    if change_pct is None:
        change_pct = 0.0

    if change_pct >= 20:     base, desc = 25, f'户数+{change_pct:.0f}%(大幅分散)'
    elif change_pct >= 10:   base, desc = 35, f'户数+{change_pct:.0f}%'
    elif change_pct >= -10:  base, desc = 50, f'户数{change_pct:+.0f}%(平稳)'
    elif change_pct >= -20:  base, desc = 65, f'户数{change_pct:.0f}%(集中)'
    else:                    base, desc = 75, f'户数{change_pct:.0f}%(大幅集中)'

    return base, desc
```

- [ ] **Step 6: 验证 14 个测试全过 (5+4+5)**

Run: `pytest 短线工具箱/tests/test_institutional_flow.py -v`
Expected: 14 个测试全 PASS

- [ ] **Step 7: Commit**

```bash
git add 短线工具箱/akshare_resilient.py 短线工具箱/institutional_flow.py 短线工具箱/tests/test_institutional_flow.py
git commit -m "feat(v2.0): add fetch_holder + score_holder (股东户数子分)"
```

---

## Task 4: 解禁 fetcher + score_restricted (TDD)

**Files:**
- Modify: `短线工具箱/akshare_resilient.py` (在 `fetch_holder` 之后追加 `fetch_restricted`)
- Modify: `短线工具箱/institutional_flow.py` (追加 `score_restricted`)
- Modify: `短线工具箱/tests/test_institutional_flow.py` (追加 4 个 score_restricted 测试)

- [ ] **Step 1: 验证 akshare 解禁接口可用性**

Run:
```bash
python -c "import akshare as ak; print([x for x in dir(ak) if 'restricted' in x.lower() or '解禁' in x or 'lift' in x.lower()][:10])"
```
Expected: 输出含 restricted 或解禁的函数名(找 `stock_restricted_release_em` 之类)。

记录:找到的接口名。

- [ ] **Step 2: 在 `akshare_resilient.py` 末尾追加 `fetch_restricted`**

在 `fetch_holder` 之后追加:

```python
def fetch_restricted(date: str | None = None) -> pd.DataFrame | None:
    """限售解禁日历(v2.0)。ak.stock_restricted_release_em 拉未来 90 日。
    字段:代码/简称/解禁日/解禁数量/占总股本比例/流通市值。
    用于:计算 30 日内解禁压力(前瞻卖压)。"""
    import akshare as ak
    return call_with_fallback(
        attempts=[
            (getattr(ak, 'stock_restricted_release_em', None),
             {'date': date} if date else {}),
            (getattr(ak, 'stock_restricted_release_sse_szse', None), {}),
            (getattr(ak, 'stock_restricted_release', None), {}),
        ],
        cache_key=f'restricted_{date or "forward90"}',
        timeout=15,
        retries=1,
    )
```

- [ ] **Step 3: 验证语法 + 可导入**

Run:
```bash
python -m py_compile 短线工具箱/akshare_resilient.py && python -c "from 短线工具箱.akshare_resilient import fetch_restricted; print('ok')"
```
Expected: 无输出 + 退出 0 + 输出 `ok`。

- [ ] **Step 4: 在 `test_institutional_flow.py` 追加 4 个 score_restricted 测试**

在文件末尾追加:

```python
# ============================================================
# score_restricted 测试
# ============================================================
def _make_restricted_row(code='600001', pct=1.0):
    """构造解禁单行(解禁数量/总股本比例,单位:%)。"""
    return {'代码': code, '占总股本比例': pct}


def test_restricted_heavy():
    """30 日内解禁 12% → 20 分(强卖压)。"""
    from 短线工具箱.institutional_flow import score_restricted
    df = pd.DataFrame([_make_restricted_row(pct=12.0)])
    df['_code6'] = df['代码']
    score, reason = score_restricted('600001', df, pct_30d=12.0)
    assert score == 20
    assert '12' in reason or '卖压' in reason or '解禁' in reason


def test_restricted_mild():
    """30 日解禁 6% → 35 分。"""
    from 短线工具箱.institutional_flow import score_restricted
    df = pd.DataFrame([_make_restricted_row(pct=6.0)])
    df['_code6'] = df['代码']
    score, reason = score_restricted('600001', df, pct_30d=6.0)
    assert score == 35


def test_restricted_none():
    """30 日解禁 0% → 75 分。"""
    from 短线工具箱.institutional_flow import score_restricted
    df = pd.DataFrame([_make_restricted_row()])
    df['_code6'] = df['代码']
    score, reason = score_restricted('600001', df, pct_30d=0.0)
    assert score == 75


def test_restricted_data_missing():
    """df=None → 50 分。"""
    from 短线工具箱.institutional_flow import score_restricted
    score, reason = score_restricted('600001', None)
    assert score == 50
    assert '缺失' in reason
```

- [ ] **Step 5: 在 `institutional_flow.py` 追加 score_restricted**

在 `score_holder` 之后追加:

```python
# ============================================================
# 子分 4: 解禁压力(未来 30 日)
# ============================================================
def score_restricted(code6: str, restricted_df: Optional[pd.DataFrame],
                     pct_30d: Optional[float] = None) -> Tuple[int, str]:
    """解禁压力子分 0-100。
    pct_30d: 未来 30 日累计解禁占总股本比例(%)。
              测试/调用方传入;若 None 则尝试从 restricted_df 计算。
    评分逻辑:
      - 解禁 >= 10% → 20 分(强卖压)
      - 解禁 5~10% → 35 分
      - 解禁 1~5% → 50 分
      - 解禁 0~1% → 65 分
      - 解禁 0% → 75 分
      - df 缺失 → 50 分
    """
    if restricted_df is None or restricted_df.empty:
        return 50, '解禁数据缺失'

    if pct_30d is None:
        pct_30d = 0.0

    if pct_30d >= 10:    base, desc = 20, f'30日解禁{pct_30d:.1f}%(强卖压)'
    elif pct_30d >= 5:   base, desc = 35, f'30日解禁{pct_30d:.1f}%'
    elif pct_30d >= 1:   base, desc = 50, f'30日解禁{pct_30d:.1f}%'
    elif pct_30d > 0:    base, desc = 65, f'30日解禁{pct_30d:.1f}%'
    else:                base, desc = 75, '30日内无解禁'

    return base, desc
```

- [ ] **Step 6: 验证 18 个测试全过 (5+4+5+4)**

Run: `pytest 短线工具箱/tests/test_institutional_flow.py -v`
Expected: 18 个测试全 PASS

- [ ] **Step 7: Commit**

```bash
git add 短线工具箱/akshare_resilient.py 短线工具箱/institutional_flow.py 短线工具箱/tests/test_institutional_flow.py
git commit -m "feat(v2.0): add fetch_restricted + score_restricted (解禁子分)"
```

---

## Task 5: compute_institutional_score 综合 (TDD)

**Files:**
- Modify: `短线工具箱/institutional_flow.py` (追加 `INST_SUB_WEIGHTS` + `compute_institutional_score`)
- Modify: `短线工具箱/tests/test_institutional_flow.py` (追加 2 个综合测试)

- [ ] **Step 1: 在 `test_institutional_flow.py` 末尾追加 2 个 compute_institutional_score 测试**

```python
# ============================================================
# compute_institutional_score 测试
# ============================================================
def test_inst_all_pass():
    """4 子分都好(75+70+65+75)→ 综合分 ≥ 70。"""
    from 短线工具箱.institutional_flow import compute_institutional_score
    # 构造所有子分都高分的情况
    dzjy = pd.DataFrame([_make_dzjy_row(rate=-8.0)])  # 溢价 8% → 80
    dzjy['_code6'] = dzjy['证券代码']
    holder = pd.DataFrame([_make_holder_row()])
    holder['_code6'] = holder['代码']
    restricted = pd.DataFrame([_make_restricted_row()])
    restricted['_code6'] = restricted['代码']
    margin = pd.DataFrame([_make_margin_row()])
    margin['_code6'] = margin['标的代码']

    score, subs = compute_institutional_score(
        '600001', dzjy, margin, holder, restricted,
        margin_decline_pct=-8.0, holder_change_pct=-25.0, restricted_pct_30d=0.0,
    )
    assert score >= 70
    assert 'dzjy' in subs and 'margin' in subs and 'holder' in subs and 'restricted' in subs


def test_inst_all_fail():
    """4 子分都差(25+25+25+20)→ 综合分 ≤ 35。"""
    from 短线工具箱.institutional_flow import compute_institutional_score
    dzjy = pd.DataFrame([_make_dzjy_row(rate=12.0, seller='机构专用席位')])  # 15
    dzjy['_code6'] = dzjy['证券代码']
    holder = pd.DataFrame([_make_holder_row()])
    holder['_code6'] = holder['代码']
    restricted = pd.DataFrame([_make_restricted_row()])
    restricted['_code6'] = restricted['代码']
    margin = pd.DataFrame([_make_margin_row()])
    margin['_code6'] = margin['标的代码']

    score, subs = compute_institutional_score(
        '600001', dzjy, margin, holder, restricted,
        margin_decline_pct=15.0, holder_change_pct=25.0, restricted_pct_30d=12.0,
    )
    assert score <= 35
```

- [ ] **Step 2: 验证测试失败 (compute_institutional_score 未实现)**

Run: `pytest 短线工具箱/tests/test_institutional_flow.py::test_inst_all_pass -v`
Expected: FAIL,`ImportError: cannot import name 'compute_institutional_score'`

- [ ] **Step 3: 在 `institutional_flow.py` 追加 INST_SUB_WEIGHTS + compute_institutional_score**

在 `score_restricted` 之后追加:

```python
# ============================================================
# 机构行为综合分
# ============================================================
INST_SUB_WEIGHTS = {
    'dzjy': 0.30,        # 大宗折价(出货信号)
    'margin': 0.25,      # 融资融券变化(杠杆撤退)
    'holder': 0.25,      # 股东户数(筹码集中度)
    'restricted': 0.20,  # 解禁压力(前瞻卖压)
}


def compute_institutional_score(
    code6: str,
    dzjy_df: Optional[pd.DataFrame],
    margin_df: Optional[pd.DataFrame],
    holder_df: Optional[pd.DataFrame],
    restricted_df: Optional[pd.DataFrame],
    margin_decline_pct: Optional[float] = None,
    holder_change_pct: Optional[float] = None,
    restricted_pct_30d: Optional[float] = None,
) -> Tuple[int, dict]:
    """机构行为综合分 0-100 = 4 子分加权。
    输入:4 张缓存表 + 3 个可选派生指标(优先用传入的派生值,避免重新计算)。
    输出:(综合分 0-100, {子分名: (score, reason)} dict)
    """
    sub_scores = {
        'dzjy':       score_dzjy(code6, dzjy_df),
        'margin':     score_margin(code6, margin_df, margin_decline_pct),
        'holder':     score_holder(code6, holder_df, holder_change_pct),
        'restricted': score_restricted(code6, restricted_df, restricted_pct_30d),
    }
    composite = sum(s * INST_SUB_WEIGHTS[k] for k, (s, _) in sub_scores.items())
    return round(composite), sub_scores
```

- [ ] **Step 4: 验证 20 个测试全过 (5+4+5+4+2)**

Run: `pytest 短线工具箱/tests/test_institutional_flow.py -v`
Expected: 20 个测试全 PASS

- [ ] **Step 5: Commit**

```bash
git add 短线工具箱/institutional_flow.py 短线工具箱/tests/test_institutional_flow.py
git commit -m "feat(v2.0): add compute_institutional_score (4子分综合)"
```

---

## Task 6: 主力分单 score_main_force_split (TDD)

**Files:**
- Modify: `短线工具箱/institutional_flow.py` (追加 `score_main_force_split`)
- Modify: `短线工具箱/tests/test_institutional_flow.py` (追加 5 个 score_main_force_split 测试)

- [ ] **Step 1: 验证 akshare 主力分单接口可用性**

Run:
```bash
python -c "import akshare as ak; print([x for x in dir(ak) if 'fund_flow' in x.lower() or '主力' in x or 'individual' in x.lower()][:15])"
```
Expected: 输出含 'fund_flow' 的函数名。重点找能拿特大/大/中/小单细分的接口(如 `stock_individual_fund_flow` 或 `stock_individual_fund_flow_rank`)。

记录:找到的接口名 + 是否提供分单字段。

- [ ] **Step 2: 在 `test_institutional_flow.py` 末尾追加 5 个 score_main_force_split 测试**

```python
# ============================================================
# score_main_force_split 测试(主力分单细粒度)
# ============================================================
def _make_fund_flow_row(code='600001', super_large=1e7, large=5e6, mid=-1e6, small=2e6):
    """构造主力分单单行(单位:元,正数=流入,负数=流出)。"""
    return {
        '代码': code,
        '特大单净额': super_large, '大单净额': large,
        '中单净额': mid, '小单净额': small,
    }


def test_split_institutional_buying():
    """特大单+大单流入,中单+小单流出 → 70 分(机构吸筹)。"""
    from 短线工具箱.institutional_flow import score_main_force_split
    df = pd.DataFrame([_make_fund_flow_row(super_large=1e8, large=5e7, mid=-1e7, small=-2e7)])
    df['_code6'] = df['代码']
    score, reason = score_main_force_split('600001', df)
    assert score >= 60
    assert '吸筹' in reason or '流入' in reason or '大单' in reason


def test_split_institutional_selling():
    """特大单+大单流出,中单+小单流入 → 30 分(派发给散户)。"""
    from 短线工具箱.institutional_flow import score_main_force_split
    df = pd.DataFrame([_make_fund_flow_row(super_large=-1e8, large=-5e7, mid=1e7, small=2e7)])
    df['_code6'] = df['代码']
    score, reason = score_main_force_split('600001', df)
    assert score <= 40
    assert '派发' in reason or '流出' in reason or '大单' in reason


def test_split_mixed():
    """特大单流入但大单流出 → 50 分(混合信号)。"""
    from 短线工具箱.institutional_flow import score_main_force_split
    df = pd.DataFrame([_make_fund_flow_row(super_large=1e8, large=-5e7, mid=1e7, small=-2e7)])
    df['_code6'] = df['代码']
    score, reason = score_main_force_split('600001', df)
    assert 40 <= score <= 60


def test_split_no_records():
    """code 不在 df 中 → 55 分。"""
    from 短线工具箱.institutional_flow import score_main_force_split
    df = pd.DataFrame([_make_fund_flow_row(code='600002')])
    df['_code6'] = df['代码']
    score, reason = score_main_force_split('600001', df)
    assert score == 55


def test_split_data_missing():
    """df=None → 50 分。"""
    from 短线工具箱.institutional_flow import score_main_force_split
    score, reason = score_main_force_split('600001', None)
    assert score == 50
    assert '缺失' in reason
```

- [ ] **Step 3: 在 `institutional_flow.py` 追加 score_main_force_split**

在 `compute_institutional_score` 之后追加:

```python
# ============================================================
# 主力分单细粒度(资金面内部子分)
# ============================================================
def score_main_force_split(code6: str, fund_flow_df: Optional[pd.DataFrame]) -> Tuple[int, str]:
    """主力分单子分 0-100,识别"机构吸筹 vs 派发给散户"。
    字段:特大单/大单/中单/小单净额(元,正数=流入)。
    评分逻辑:
      - 特大+大单净流入 且 中+小单净流出 → 70 分(典型吸筹)
      - 特大+大单净流入 → 60 分
      - 特大+大单净流出 且 中+小单净流入 → 30 分(典型派发)
      - 特大+大单净流出 → 40 分
      - 混合(单边不一致) → 50 分
      - code 不在 df → 55 分
      - df 缺失 → 50 分
    """
    if fund_flow_df is None or fund_flow_df.empty:
        return 50, '主力分单数据缺失'

    code_col = '_code6' if '_code6' in fund_flow_df.columns else '代码'
    rows = fund_flow_df[fund_flow_df[code_col].astype(str).str.zfill(6) == str(code6).zfill(6)]
    if rows.empty:
        return 55, '该票无主力分单数据'

    # 兼容列名(特大单/大单/中单/小单 各有别名)
    def _get_col(df, keywords):
        for c in df.columns:
            for kw in keywords:
                if kw in str(c):
                    return c
        return None

    super_col = _get_col(rows, ['特大单'])
    large_col = _get_col(rows, ['大单', '大单净额'])
    mid_col   = _get_col(rows, ['中单'])
    small_col = _get_col(rows, ['小单'])

    if not (super_col and large_col and mid_col and small_col):
        return 50, '主力分单缺列'

    super_amt = float(pd.to_numeric(rows[super_col], errors='coerce').iloc[0] or 0)
    large_amt = float(pd.to_numeric(rows[large_col], errors='coerce').iloc[0] or 0)
    mid_amt   = float(pd.to_numeric(rows[mid_col], errors='coerce').iloc[0] or 0)
    small_amt = float(pd.to_numeric(rows[small_col], errors='coerce').iloc[0] or 0)

    # 主力 = 特大+大单
    main_amt = super_amt + large_amt
    retail_amt = mid_amt + small_amt

    if main_amt > 0 and retail_amt < 0:
        score, desc = 70, f'主力+{main_amt/1e6:.0f}万 散户{retail_amt/1e6:.0f}万(吸筹)'
    elif main_amt > 0:
        score, desc = 60, f'主力+{main_amt/1e6:.0f}万(流入)'
    elif main_amt < 0 and retail_amt > 0:
        score, desc = 30, f'主力{main_amt/1e6:.0f}万 散户+{retail_amt/1e6:.0f}万(派发)'
    elif main_amt < 0:
        score, desc = 40, f'主力{main_amt/1e6:.0f}万(流出)'
    else:
        score, desc = 50, '主力分单持平'

    return score, desc
```

- [ ] **Step 4: 验证 25 个测试全过 (5+4+5+4+2+5)**

Run: `pytest 短线工具箱/tests/test_institutional_flow.py -v`
Expected: 25 个测试全 PASS

- [ ] **Step 5: Commit**

```bash
git add 短线工具箱/institutional_flow.py 短线工具箱/tests/test_institutional_flow.py
git commit -m "feat(v2.0): add score_main_force_split (主力分单细粒度)"
```

---

## Task 7: v1.py 接入 _init_institutional_flow

**Files:**
- Modify: `筛选明日股票_v1.py`:
  - 第 51 行附近: importlib 循环追加 4 个 fetch + 1 个新模块
  - 第 825 行后(v1.3 `_init_emotion_and_zt_pool` 之后): 追加 `_INST_DF` 缓存 + `_init_institutional_flow()`

- [ ] **Step 1: 在 v1.py 顶部 import 段追加 4 个 fetch**

打开 `筛选明日股票_v1.py`,找到第 51 行附近的循环:
```python
for _k in ["fetch_earnings","fetch_notice","fetch_lhb","fetch_lhb_inst","fetch_fund_flow_rank","fetch_hsgt","fetch_news","fetch_zt_pool","fetch_market_activity"]: locals()[_k] = getattr(_res_mod, _k)
```

替换为:
```python
for _k in ["fetch_earnings","fetch_notice","fetch_lhb","fetch_lhb_inst","fetch_fund_flow_rank","fetch_hsgt","fetch_news","fetch_zt_pool","fetch_market_activity",
           # v2.0: 机构流出 4 个新数据源
           "fetch_dzjy","fetch_margin","fetch_holder","fetch_restricted"]:
    locals()[_k] = getattr(_res_mod, _k)
```

- [ ] **Step 2: 在 v1.py 顶部 importlib 段追加新模块**

找到现有的 institutional 模块 import 段(参考 v1.3 `_me_spec = _ilu.spec_from_file_location("market_emotion", ...)`),在它**之后**追加:

```python
# v2.0 机构行为模块
_if_spec = _ilu.spec_from_file_location("institutional_flow",
    os.path.join(os.path.dirname(__file__), "短线工具箱", "institutional_flow.py"))
_if_mod = _ilu.module_from_spec(_if_spec)
_if_spec.loader.exec_module(_if_mod)
for _n in ["compute_institutional_score", "score_dzjy", "score_margin",
           "score_holder", "score_restricted", "score_main_force_split",
           "INST_SUB_WEIGHTS"]:
    locals()[_n] = getattr(_if_mod, _n)
```

- [ ] **Step 3: 找到 v1.3 `_init_emotion_and_zt_pool` 函数末尾(第 854 行附近 `_EMOTION_TIER = compute_emotion_tier(...)` 之后),追加 _init_institutional_flow**

```python
# === v2.0 机构流出缓存 + 预拉 ===
_INST_DF = {'dzjy': None, 'margin': None, 'holder': None, 'restricted': None}


def _init_institutional_flow():
    """v2.0: 预拉 4 张机构流出表。任一失败 → 该子分 50 兜底。"""
    global _INST_DF
    if _INST_DF['dzjy'] is not None or _INST_DF['margin'] is not None:
        return _INST_DF
    for key, fn in [('dzjy', fetch_dzjy), ('margin', fetch_margin),
                    ('holder', fetch_holder), ('restricted', fetch_restricted)]:
        try:
            df = fn()
            if df is not None and not df.empty:
                # 统一 _code6 列(若 df 还没加)
                if '_code6' not in df.columns:
                    cc = next((c for c in df.columns if '代码' in str(c)), None)
                    if cc:
                        df['_code6'] = df[cc].astype(str).str.zfill(6)
                _INST_DF[key] = df
                print(f'[预拉] 机构-{key} {len(df)} 条')
            else:
                print(f'[预拉] 机构-{key} 拉取为空(子分兜底 50)')
        except Exception as e:
            print(f'[预拉] 机构-{key} 失败:{type(e).__name__}(子分兜底 50)')
    return _INST_DF
```

- [ ] **Step 4: 在主流程 `_init_emotion_and_zt_pool()` 调用之后,加 `_init_institutional_flow()` 调用**

找到主流程中类似这样的段:
```python
_init_emotion_and_zt_pool()
```

在它**之后**追加:
```python
_init_institutional_flow()
```

- [ ] **Step 5: 验证语法 + 可导入 + 现有 pytest 不回归**

Run:
```bash
python -m py_compile 筛选明日股票_v1.py && python -c "import 筛选明日股票_v1; print('ok')" && pytest 短线工具箱/tests/ -v --ignore=短线工具箱/tests/test_p1_lead_signal.py
```
Expected: 退出 0 + 输出 `ok` + 旧测试(除 test_p1_lead_signal.py 已知失败外)全 PASS

- [ ] **Step 6: Commit**

```bash
git add 筛选明日股票_v1.py
git commit -m "feat(v2.0): wire institutional_flow module + _init_institutional_flow()"
```

---

## Task 8: v1.py 资金面 5 子分重构 (get_capital_flow_score_v2)

**Files:**
- Modify: `筛选明日股票_v1.py`:
  - 在 `get_capital_flow_score` 函数定义之后(第 612 行附近)追加 `get_capital_flow_score_v2`
  - 暂不替换调用点(Task 9 一起换)

- [ ] **Step 1: 在 `get_capital_flow_score` 函数末尾(第 612 行)之后追加 v2 版本**

```python
def get_capital_flow_score_v2(code):
    """v2.0 资金面综合分(5 子分加权):
        主力5日 × 0.20 + 主力分单 × 0.30 + 北向3日 × 0.20
      + 龙虎榜+机构席位 × 0.20 + 量比 × 0.10
    """
    # 1. 主力 5 日净额(原 get_capital_flow_score,权重 0.20)
    base_score, base_reason = get_capital_flow_score(code)
    # 2. 主力分单细粒度(新,权重 0.30)
    fen_score, fen_reason = score_main_force_split(
        code, globals().get('_FUND_FLOW_DF', None))
    # 3. 北向 3 日(权重 0.20)
    hsgt_score, hsgt_reason = get_hsgt_score(code)
    # 4. 龙虎榜 + 机构席位(权重 0.20)
    lhb_score, lhb_reason = get_lhb_score(code)
    # 5. 量比(权重 0.10)
    amt_score, amt_reason = _get_amount_ratio_score(code)

    composite = round(
        base_score * 0.20 + fen_score * 0.30 + hsgt_score * 0.20 +
        lhb_score * 0.20 + amt_score * 0.10)
    reason = (f"主力5日{base_score}|分单{fen_score}|"
              f"北向{hsgt_score}|龙虎{lhb_score}|量比{amt_score}")
    return composite, reason


def _get_amount_ratio_score(code):
    """量比子分 0-100:基于今日 amount vs 5 日均量比。
    与 get_capital_flow_score 内部退化路径相同逻辑。"""
    df = globals().get('histories', {}).get(code)
    if df is None or len(df) < 6:
        return 50, '量比数据缺失'
    today_amt = float(df['amount'].iloc[-1])
    avg_5d = float(df['amount'].iloc[-6:-1].mean())
    if avg_5d <= 0:
        return 50, '量比基线为0'
    ratio = today_amt / avg_5d
    if ratio >= 2.0:    return 85, f'放量{ratio:.1f}x'
    elif ratio >= 1.5:  return 70, f'放量{ratio:.1f}x'
    elif ratio >= 0.85: return 50, f'量平稳{ratio:.2f}x'
    elif ratio >= 0.6:  return 35, f'缩量{ratio:.2f}x'
    else:               return 20, f'严重缩量{ratio:.2f}x'
```

- [ ] **Step 2: 在 `_init_institutional_flow` 函数末尾追加 `_FUND_FLOW_DF` 全局变量(如果主流程需要)**

在 `_init_institutional_flow` 末尾(Step 3 之后)追加 fetch_fund_flow_rank 预拉:

找到 v1.py 现有 `fetch_fund_flow_rank` 调用点(在 `_load_capital_flow_table` 函数内,line ~570),在它**之后**添加:
```python
# v2.0 主力分单表预拉(供 score_main_force_split 用)
_FUND_FLOW_DF = None
```

并在 `_init_institutional_flow()` 末尾(在 `return _INST_DF` 之前)追加:
```python
    # 主力分单表预拉
    try:
        ff_df = fetch_fund_flow_rank(indicator='今日')
        if ff_df is not None and not ff_df.empty:
            if '_code6' not in ff_df.columns:
                cc = next((c for c in ff_df.columns if '代码' in str(c)), None)
                if cc:
                    ff_df['_code6'] = ff_df[cc].astype(str).str.zfill(6)
            globals()['_FUND_FLOW_DF'] = ff_df
            print(f'[预拉] 主力分单表 {len(ff_df)} 条')
    except Exception as e:
        print(f'[预拉] 主力分单表 失败:{type(e).__name__}(分单兜底 50)')
```

- [ ] **Step 3: 验证语法 + 可导入 + 现有 pytest 不回归**

Run:
```bash
python -m py_compile 筛选明日股票_v1.py && python -c "import 筛选明日股票_v1; print('ok'); print(筛选明日股票_v1.get_capital_flow_score_v2('sh.600000'))"
```
Expected: 退出 0 + 输出 `ok` + 一个元组(分数, reason 字符串)。**注意**:`get_capital_flow_score_v2` 调用会触发各子分 fetch,在测试环境可能 None 兜底 → score=50,reason=多个 '缺失'。这是预期的。

- [ ] **Step 4: Commit**

```bash
git add 筛选明日股票_v1.py
git commit -m "feat(v2.0): add get_capital_flow_score_v2 (资金面 5 子分)"
```

---

## Task 9: v1.py 消息面简化 (2 子分) + 5 维评分 compute_composite_v2

**Files:**
- Modify: `筛选明日股票_v1.py`:
  - 第 896-920 行附近(`get_news_catalyst_score` + `NEWS_SUB_WEIGHTS`):从 4 子缩到 2 子
  - 在 `get_news_catalyst_score` 之后追加 `compute_composite_v2`

- [ ] **Step 1: 找到 v1.py 现有 `NEWS_SUB_WEIGHTS` 常量(line ~896)**

```python
NEWS_SUB_WEIGHTS = {'ann': 0.30, 'newsfeed': 0.30, 'hsgt': 0.20, 'lhb': 0.20}
```

替换为:
```python
NEWS_SUB_WEIGHTS = {'ann': 0.50, 'newsfeed': 0.50}  # v2.0: hsgt/lhb 移至资金面
```

- [ ] **Step 2: 找到 `get_news_catalyst_score` 函数定义(line ~898-921),移除 hsgt/lhb 引用**

在函数体内,删除(或注释掉)以下 2 行(如果存在):
```python
hsgt_sub, hsgt_reason = get_hsgt_score(code)
lhb_sub, lhb_reason = get_lhb_score(code)
```

并将函数末尾的 `score = ...` 计算公式从
```python
score = (ann_sub * NEWS_SUB_WEIGHTS['ann'] + ...)
```
改为
```python
score = (ann_sub * NEWS_SUB_WEIGHTS['ann'] + newsfeed_sub * NEWS_SUB_WEIGHTS['newsfeed'])
```

并更新 reason 字符串,移除 `|北向{hsgt_sub}|龙虎{lhb_sub}` 部分。

- [ ] **Step 3: 在 `get_news_catalyst_score` 函数之后追加 `compute_composite_v2`**

```python
# === v2.0 5 维评分 ===
WEIGHT_V2 = {
    'tech':  0.20,
    'earn':  0.15,
    'flow':  0.25,
    'news':  0.20,
    'inst':  0.20,
}


def compute_composite_v2(code, tech_score, earn_score, flow_score, news_score):
    """5 维加权综合分。机构行为子分从 _INST_DF + _init_institutional_flow() 派生。"""
    # 机构行为子分(从缓存表 + 派生指标)
    inst_dfs = _init_institutional_flow()
    inst_score, inst_subs = compute_institutional_score(
        code,
        inst_dfs.get('dzjy'),
        inst_dfs.get('margin'),
        inst_dfs.get('holder'),
        inst_dfs.get('restricted'),
    )
    composite = round(
        tech_score * WEIGHT_V2['tech'] +
        earn_score * WEIGHT_V2['earn'] +
        flow_score * WEIGHT_V2['flow'] +
        news_score * WEIGHT_V2['news'] +
        inst_score * WEIGHT_V2['inst']
    )
    return composite, {
        'tech': tech_score, 'earn': earn_score,
        'flow': flow_score, 'news': news_score, 'inst': inst_score,
        'inst_subs': inst_subs,  # 子分明细,给报告用
    }
```

- [ ] **Step 4: 验证语法 + 可导入**

Run:
```bash
python -m py_compile 筛选明日股票_v1.py && python -c "import 筛选明日股票_v1; print('ok'); print(筛选明日股票_v1.compute_composite_v2('sh.600000', 60, 50, 50, 50))"
```
Expected: 退出 0 + 输出 `ok` + 一个元组(综合分 0-100, dict 含 5 维子分)

- [ ] **Step 5: Commit**

```bash
git add 筛选明日股票_v1.py
git commit -m "feat(v2.0): simplify news sub-weights + add compute_composite_v2 (5 维)"
```

---

## Task 10: v1.py compute_falsification_signals 加 3 条机构流出

**Files:**
- Modify: `筛选明日股票_v1.py`:
  - `compute_falsification_signals` 函数定义(第 1550 行附近):多收 3 个 inst 参数 + 追加 3 条证伪信号

- [ ] **Step 1: 找到 `compute_falsification_signals` 函数签名(line ~1550)**

```python
def compute_falsification_signals(r):
```

替换为:
```python
def compute_falsification_signals(r, inst_dzjy_rate=None, inst_margin_change=None, inst_restricted_30d=None):
    """v2.0: 多收 3 个机构行为参数,追加 3 条证伪信号。"""
```

- [ ] **Step 2: 在函数体内追加 3 条新证伪信号(在现有 9 条之后)**

```python
    # === v2.0 机构流出相关 3 条 ===
    if inst_dzjy_rate is not None and inst_dzjy_rate >= 10:
        sigs.append(('高', f'大宗折价{inst_dzjy_rate:.1f}%(强出货信号)'))
    if inst_margin_change is not None and inst_margin_change <= -10:
        sigs.append(('中', f'融资余额周降{abs(inst_margin_change):.1f}%(杠杆踩踏)'))
    if inst_restricted_30d is not None and inst_restricted_30d >= 5:
        sigs.append(('中', f'30日内解禁{inst_restricted_30d:.1f}%流通(前瞻卖压)'))
```

- [ ] **Step 3: 找到所有调用 `compute_falsification_signals(r)` 的地方(grep),改调用方式**

Run: `grep -n "compute_falsification_signals" 筛选明日股票_v1.py`

Expected: 多个调用点(可能在 TOP3 输出函数内)。

对每个调用点,改为:
```python
falsifications = compute_falsification_signals(
    r,
    inst_dzjy_rate=<from inst_subs['dzjy'][1] parsed>,
    inst_margin_change=<from inst_subs['margin'][1] parsed>,
    inst_restricted_30d=<from inst_subs['restricted'][1] parsed>,
)
```

**具体解析逻辑**:从 `inst_subs['dzjy'][1]`(reason 字符串)用正则提取折价率数字。如 `"大宗折价12.3%(强出货)+机构席位" → 12.3`。

简化:在 `compute_composite_v2` 调用的同时,把派生指标(margin_decline_pct, holder_change_pct, restricted_pct_30d)也传过去,然后 falsification 直接用这些值。

**实际修改建议**:把 `compute_falsification_signals` 改为接收原始派生指标而不是从 reason 字符串解析:

```python
def compute_falsification_signals(r, inst_metrics=None):
    """inst_metrics: dict 含 'dzjy_rate', 'margin_change', 'restricted_30d'。"""
    if inst_metrics:
        dzjy_rate = inst_metrics.get('dzjy_rate')
        margin_change = inst_metrics.get('margin_change')
        restricted_30d = inst_metrics.get('restricted_30d')
        if dzjy_rate is not None and dzjy_rate >= 10:
            sigs.append(('高', f'大宗折价{dzjy_rate:.1f}%(强出货信号)'))
        if margin_change is not None and margin_change <= -10:
            sigs.append(('中', f'融资余额周降{abs(margin_change):.1f}%(杠杆踩踏)'))
        if restricted_30d is not None and restricted_30d >= 5:
            sigs.append(('中', f'30日内解禁{restricted_30d:.1f}%流通(前瞻卖压)'))
```

调用点改为:
```python
falsifications = compute_falsification_signals(
    r,
    inst_metrics={
        'dzjy_rate': inst_dzjy_rate,  # 从 _init_institutional_flow 派生
        'margin_change': inst_margin_change,
        'restricted_30d': inst_restricted_30d,
    },
)
```

**派生指标计算**:在 v1.py 主流程中,Top3 输出前,从 `_INST_DF` 算出每只票的 3 个派生指标(可以简化为先用 None,后续跑 daily.py 时再补):

```python
# 简化:第 1 版传 None,占位证伪(无派生指标时 3 条新证伪不触发)
# v2.0.1 后续从 _INST_DF 派生 dzjy_rate/margin_change/restricted_30d,见后续 Task 14
inst_metrics = {
    'dzjy_rate': None,
    'margin_change': None,
    'restricted_30d': None,
}
falsifications = compute_falsification_signals(r, inst_metrics=inst_metrics)
```

- [ ] **Step 4: 验证语法 + 可导入**

Run:
```bash
python -m py_compile 筛选明日股票_v1.py && python -c "import 筛选明日股票_v1; print('ok')"
```
Expected: 退出 0 + 输出 `ok`

- [ ] **Step 5: 现有 pytest 不回归**

Run: `pytest 短线工具箱/tests/test_scorer.py -v`
Expected: 全 PASS(无回归)

- [ ] **Step 6: Commit**

```bash
git add 筛选明日股票_v1.py
git commit -m "feat(v2.0): add 3 机构流出证伪门 (大宗/融资/解禁)"
```

---

## Task 11: v1.py TOP3 卡片输出加 1 行机构行为子分

**Files:**
- Modify: `筛选明日股票_v1.py`:
  - 找到 TOP3 卡片打印函数(打印"5 维子分:技术 X | 业绩 Y | ..."的位置)
  - 加 1 行机构行为子分 + 主力分单细粒度说明

- [ ] **Step 1: grep 找 TOP3 卡片输出位置**

Run: `grep -n "4 维子分\|5 维子分\|技术.*业绩.*资金.*消息" 筛选明日股票_v1.py`

Expected: 1-2 处(CLI 打印 + HTML 报告)。

- [ ] **Step 2: 在 CLI 打印函数内,5 维子分行改为 5 列(加 '机构')**

原:
```python
print(f"5 维子分:技术 {tech} | 业绩 {earn} | 资金 {flow} | 消息 {news}")
```

改为(在 tech/earn/flow/news 之后加 '机构' 字段):
```python
print(f"5 维子分:技术 {tech} | 业绩 {earn} | 资金 {flow} | 消息 {news} | 机构 {inst}")
```

- [ ] **Step 3: 在 `说明:` 行(描述业绩/资金/消息原因)后追加"主力分单"说明**

找到类似:
```python
说明:业绩平稳 | 缩量0.7x | 无新催化
```

在末尾追加 `| 大单流出-2.1亿 / 小单流入+0.8亿` (示例,实际从 `score_main_force_split` reason 取):

```python
说明:业绩平稳 | 缩量0.7x | 无新催化 | {fen_reason}
```

其中 `fen_reason = score_main_force_split(code, _FUND_FLOW_DF)[1]`

- [ ] **Step 4: HTML 报告集成(参考 v1.3 集成模式)**

在 HTML 报告生成函数中,找到 "4 维子分柱状图" 部分,改为 "5 维子分柱状图",加 1 列 "机构行为"。

简化:如果 HTML 生成代码复杂,只改 CLI 打印,HTML 留为 v2.0.1 后续 patch。

- [ ] **Step 5: 验证语法 + 可导入 + 跑一次 daily.py --help**

Run:
```bash
python -m py_compile 筛选明日股票_v1.py && python -c "import 筛选明日股票_v1; print('ok')" && python daily.py --help
```
Expected: 退出 0 + 输出 `ok` + `daily.py --help` 正常

- [ ] **Step 6: Commit**

```bash
git add 筛选明日股票_v1.py
git commit -m "feat(v2.0): add 机构行为子分 to TOP3 CLI/HTML output"
```

---

## Task 12: README.md 更新 §5 维评分体系 + §路线图

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 找到 §4 维评分体系表格(line 28-37)**

原表格(参考 README line 28-37):
```markdown
## 4 维评分体系
| 维度 | 权重 T+1 / T+0 | 含什么 | 数据源 |
|------|------|------|------|
| 技术 | 30 / 50 | ... | ... |
| 业绩 | 25 / 15 | ... | ... |
| 资金 | 20 / 25 | ... | ... |
| 消息 | 25 / 10 | ... | ... |
```

整体替换为:
```markdown
## 5 维评分体系 (v2.0 · 2026-06-15 加)

| 维度 | 权重 T+1 / T+0 | 含什么 | 数据源 |
|------|------|------|------|
| 技术 | 20 / 35 | MA5/10/20 排列、MACD 红柱、RSI、布林带位置、放量突破、量价共振 | baostock 日 K |
| 业绩 | 15 / 10 | 一季报扣非净利润同比、业绩高增加分、业绩雷区(-30%)直接淘汰 | akshare `stock_yjbb_em` |
| 资金 | 25 / 30 | 5 日主力净流入 + **主力分单(特大/大/中/小单)** + 北向 3 日 + 龙虎榜 + 量比 | akshare 多源 |
| 消息 | 20 / 10 | 公告(减持/重组/订单等)+ 新闻情感 | akshare 多源 |
| **机构行为 (新)** | **20 / 15** | **大宗折价 + 卖方席位 + 融资融券变化 + 股东户数变化 + 解禁压力** | **akshare 多源** |

每个子维度 0-100 分,加权得**综合分 0-100**。综合分 ≥ 75 = 强候选,≥ 65 = 备选。

> **资金面内部子分权重**:主力5日 0.20 + 主力分单 0.30 + 北向3日 0.20 + 龙虎榜 0.20 + 量比 0.10
> **机构行为子分权重**:大宗 0.30 + 融资 0.25 + 股东户数 0.25 + 解禁 0.20
```

- [ ] **Step 2: 找到 §路线图(README 末尾)**

在路线图表格中加 v2.0 行(在 v1.3 之后):

```markdown
| **v2.0** | **🚧 进行中 (2026-06-15)** | **5 维评分 + 机构流出深层次信号(大宗/主力分单/融资融券/股东户数+解禁压力)** |
```

- [ ] **Step 3: 找到 §证伪门章节(line 53-80),追加 3 条 v2.0 新增的证伪条件**

在 9 条表格后追加:
```markdown
| 条件 | 触发阈值 | 严重度 |
|------|---------|------|
| **大宗折价过高** (v2.0 新) | 大宗折价率 >= 10% | 高 |
| **融资余额骤降** (v2.0 新) | 7 日融资余额变化率 <= -10% | 中 |
| **解禁压力** (v2.0 新) | 30 日内累计解禁 >= 5% 流通 | 中 |
```

- [ ] **Step 4: 验证**

Run:
```bash
git diff --stat README.md && grep -c "v2.0" README.md
```
Expected: README.md 有 diff + `grep -c "v2.0"` ≥ 4(出现 4+ 次:章节标题、表格行、证伪门、路线图)

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs(readme): add v2.0 5-dim + 机构流出信号 section"
```

---

## Task 13: 自检全套 + AGENT_LOG + 最终 commit

**Files:**
- Modify: `AGENT_LOG.md` (追加历史事件,本任务执行后)
- 无代码改动

- [ ] **Step 1: 跑全套自检(pytest + py_compile + import + --help)**

Run:
```bash
cd "C:\Users\yan\Desktop\短线操作"
python -m py_compile 筛选明日股票_v1.py
python -c "import 筛选明日股票_v1; print('import ok')"
pytest 短线工具箱/tests/test_institutional_flow.py -v
pytest 短线工具箱/tests/test_scorer.py -v
pytest 短线工具箱/tests/test_market_emotion.py -v
pytest 短线工具箱/tests/test_dabang_pool.py -v
python daily.py --help
```

Expected: 所有命令退出 0
- 25 个 institutional_flow 测试全 PASS
- test_scorer / test_market_emotion / test_dabang_pool 旧测试全 PASS(无回归)
- daily.py --help 输出 usage

- [ ] **Step 2: 检查 v1.py 净行数(目标 ≤ +100 行)**

Run:
```bash
cd "C:\Users\yan\Desktop\短线操作"
git diff HEAD~10 --stat 筛选明日股票_v1.py
wc -l 筛选明日股票_v1.py
```

Expected: 净增 ≤ 100 行(从 2254 起点 → ≤ 2354)。如果超过,**暂停并优化**:考虑把 compute_composite_v2 抽到 institutional_flow.py。

- [ ] **Step 3: 追加 AGENT_LOG.md 历史事件**

打开 `AGENT_LOG.md`,在 § 历史事件 顶部追加:

```markdown
- **2026-06-15 16:00 · Codex · v2.0 实现 · feat · `<sha>`**
  备注: 5维评分 + 4个机构流出信号(大宗折价/主力分单/融资融券/股东户数+解禁)。
  13 个 commit, 每 commit 单一职责, 全套 pytest 通过(25+12+4+8=49 用例)。
  净增 v1.py <100 行, 核心逻辑在 institutional_flow.py。
  spec 偏离: [如果实施中有偏离,在此列出]
  下次接手: 跑 daily.py --backtest 验证相关性 ≥0.2, 跑 30 笔后调权重。
```

- [ ] **Step 4: 验证 git 状态干净(只 AGENT_LOG.md modified)**

Run:
```bash
cd "C:\Users\yan\Desktop\短线操作"
git status
```

Expected: working tree 中只有 `AGENT_LOG.md` 改动(已 gitignored,不会进 commit)。

- [ ] **Step 5: 不 push!等用户拍板**

**重要**: 严格按 AGENTS.md §9.5,**AI 不擅自 push**。本任务到此结束,**把 v2.0 的所有 commit 展示给用户,等用户说"OK push"再 git push**。

Run:
```bash
cd "C:\Users\yan\Desktop\短线操作"
git log --oneline -15
```

展示给用户,提示:
- v2.0 13 个 commit 已就位
- 49 个测试全过
- 等用户拍板 push

---

## Spec 覆盖自检

| Spec 章节 | 对应 Task |
|----------|----------|
| §1.2 目标(4 个机构流出信号) | Task 1, 2, 3, 4 |
| §2.1 5 维评分架构 | Task 9 (compute_composite_v2) |
| §2.1 资金面 5 子分 | Task 8 (get_capital_flow_score_v2) |
| §2.1 消息面 2 子分 | Task 9 (NEWS_SUB_WEIGHTS 简化) |
| §2.1 机构行为 20% 权重 | Task 9 (WEIGHT_V2) |
| §2.2 akshare_resilient 4 个 fetch | Task 1, 2, 3, 4 |
| §2.3 institutional_flow 4 sub-score + 综合 | Task 1, 2, 3, 4, 5 |
| §2.4a import 接入 | Task 7 (Step 1, 2) |
| §2.4b _init 预拉 | Task 7 (Step 3, 4) |
| §2.4c 5 维评分 | Task 9 (compute_composite_v2) |
| §2.4d 资金面 5 子 | Task 8 (get_capital_flow_score_v2) |
| §2.4e TOP3 卡片 + 1 行 | Task 11 |
| §2.4f 证伪门 +3 条 | Task 10 |
| §2.5 错误处理矩阵 | Task 7-11 各 fetch 失败兜底 50 |
| §3.1 单元测试 ≥20 | Task 1-6 累计 25 个测试 |
| §4 文档更新 | Task 12 (README.md) |

✅ **全覆盖**。

---

## 已知遗留 / 后续 Task

1. **回测归因**(`backtest_dimension_attribution` 扩展):等 ≥20 笔历史数据后,Task 14 单独做(本 plan 不含)。
2. **HTML 报告 5 维可视化**:Task 11 Step 4 简化版(只改 CLI),HTML 柱状图留 v2.0.1。
3. **akshare 接口名实际可用性**:本 plan 用了 3 个备选 `getattr(ak, ..., None)`,实施时若发现全部不存在,需 web 搜索替代源。
4. **派生指标实时计算**(Task 10 占位 None → 实际从 margin_df / holder_df / restricted_df 算):v2.0.1 优化。
5. **节假日/周末跑兜底**:各 fetch 已实现 df=None → score 50 兜底,无需额外处理。

---

*Plan 写完。下一步:用户审阅 → 选择执行方式(Subagent-Driven / Inline)。*
